# MHP 档期预约平台 — Linux 服务器完整部署指南

> **适用环境：** Ubuntu 20.04+，2 核 8GB。
> **服务器用户：** ubuntu（非 root，命令需要 sudo）
> **项目根目录：** `/home/ubuntu/BookingSystem`
> **访问方式：** 部署完成后通过服务器公网 IP 访问，Nginx 监听 80 端口。
> **预计耗时：** 首次约 45~90 分钟（主要是镜像下载和 ES 插件安装）。

---

## 一、连接服务器 & 安装基础软件

### 1.1 SSH 连接

```bash
ssh ubuntu@<你的服务器公网IP>
```

### 1.2 安装 Docker

```bash
# 一键安装（官方脚本）
curl -fsSL https://get.docker.com | sudo sh

# 把当前用户加入 docker 组（这样不用每次加 sudo）
sudo usermod -aG docker $USER

# 重新登录一次让组权限生效（或执行 newgrp docker）
newgrp docker

# 启动 Docker 并设置开机自启
sudo systemctl start docker
sudo systemctl enable docker

# 验证
docker -v
```

### 1.3 安装 Java 21

```bash
sudo apt-get update && sudo apt-get install -y openjdk-21-jdk

# 验证
java -version    # 应显示 openjdk 21.x
```

### 1.4 安装 Maven

```bash
cd /opt
sudo wget https://mirrors.tuna.tsinghua.edu.cn/apache/maven/maven-3/3.9.6/binaries/apache-maven-3.9.6-bin.tar.gz
sudo tar -xzf apache-maven-3.9.6-bin.tar.gz
sudo ln -s /opt/apache-maven-3.9.6 /opt/maven

# 配置环境变量
echo 'export MAVEN_HOME=/opt/maven' >> ~/.bashrc
echo 'export PATH=$MAVEN_HOME/bin:$PATH' >> ~/.bashrc
source ~/.bashrc

# 验证
mvn -v
```

配置阿里云 Maven 镜像（加速依赖下载）：

```bash
sudo cat > /opt/maven/conf/settings.xml << 'EOF'
<?xml version="1.0" encoding="UTF-8"?>
<settings>
  <mirrors>
    <mirror>
      <id>aliyun</id>
      <mirrorOf>central</mirrorOf>
      <name>aliyun</name>
      <url>https://maven.aliyun.com/repository/public</url>
    </mirror>
  </mirrors>
</settings>
EOF
```

### 1.5 安装 Node.js 18

```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo bash -
sudo apt-get install -y nodejs

# 验证
node -v    # v18.20.8（通过 NodeSource 源安装，系统自带 v12 版本过低，必须替换）
npm -v     # 10.8.2（随 Node 18 自动升级，无需单独安装）
```

> **注意：** Ubuntu 20.04 系统自带 Node.js 12，版本过低，必须通过 NodeSource 源安装 v18。安装后 npm 会随之从 8.x 升级到 10.x，行为基本兼容，无需额外处理。

### 1.6 安装 Nginx

```bash
sudo apt-get install -y nginx
sudo systemctl start nginx
sudo systemctl enable nginx
```

---

## 二、项目代码

代码已通过 Git 克隆到服务器，当前路径：

```
/home/ubuntu/BookingSystem/
├── BookSystem/          # Java 后端（Maven 多模块）
│   ├── mhp-common/
│   ├── mhp-gateway/
│   ├── mhp-account/
│   ├── mhp-booking/
│   ├── mhp-social/
│   ├── mhp-app/         # 公共资源（SQL 脚本等）
│   └── pom.xml
├── cosplay-frontend/    # Vue 前端
├── docker-compose.yml
└── .env                 # 密钥配置
```

---

## 三、配置密钥和七牛云

### 3.1 DeepSeek API Key

```bash
cd /home/ubuntu/BookingSystem
cp .env.example .env
nano .env
```

内容改为：
```
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxx
```

### 3.2 七牛云配置（文件上传功能，不用可跳过）

```bash
nano /home/ubuntu/BookingSystem/BookSystem/mhp-social/src/main/resources/application.yaml
```

找到以下 4 行，填入真实值：
```yaml
qiniu:
  access-key: 你的AccessKey
  secret-key: 你的SecretKey
  bucket: 你的Bucket名称
  domain: cdn.你的域名.com
```

---

## 四、启动中间件（Docker Compose）

### 4.1 首次构建 Elasticsearch 镜像

ES 镜像需要安装 IK 分词器 + 拼音分词器，需要联网下载，约 5~10 分钟：

```bash
cd /home/ubuntu/BookingSystem
docker compose build elasticsearch
```

> 只需首次执行，之后不用再 build。

### 4.2 启动所有中间件

```bash
docker compose up -d
```

等待约 1 分钟，查看状态：

```bash
docker compose ps
```

所有容器应为 `running`（Canal 可能显示 `restarting`，正常，等 MySQL 健康后自动稳定）。

| 容器 | 说明 | 默认账密 |
|------|------|----------|
| mhp-mysql | MySQL 8.0 | root / 222333dyh |
| mhp-redis | Redis 7 | 无 |
| mhp-rabbitmq | RabbitMQ | admin / 123456 |
| mhp-nacos | Nacos 2.3 | nacos / nacos |
| mhp-xxl-job | XXL-Job Admin | admin / 123456 |
| mhp-elasticsearch | ES 8.13 | 无（安全认证已关闭） |
| mhp-kibana | Kibana | 无 |
| mhp-canal | Canal | 无 |
| mhp-ai | AI Agent | DeepSeek API Key |

---

## 五、初始化数据库

### 5.1 初始化 xxl_job 库

XXL-Job Admin 需要一个 `xxl_job` 数据库，docker-compose 只创建了 `mhp`，需手动初始化：

```bash
docker exec -it mhp-mysql mysql -uroot -p222333dyh
```

在 MySQL 命令行中执行（复制粘贴全部内容）：

```sql
CREATE DATABASE IF NOT EXISTS xxl_job DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE xxl_job;

CREATE TABLE `xxl_job_lock` (
  `lock_name` varchar(50) NOT NULL,
  PRIMARY KEY (`lock_name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `xxl_job_group` (
  `id` int NOT NULL AUTO_INCREMENT,
  `app_name` varchar(64) NOT NULL,
  `title` varchar(12) NOT NULL,
  `address_type` tinyint NOT NULL DEFAULT '0',
  `address_list` text,
  `update_time` datetime DEFAULT NULL,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `xxl_job_info` (
  `id` int NOT NULL AUTO_INCREMENT,
  `job_group` int NOT NULL,
  `job_desc` varchar(255) NOT NULL,
  `add_time` datetime DEFAULT NULL,
  `update_time` datetime DEFAULT NULL,
  `author` varchar(64) DEFAULT NULL,
  `alarm_email` varchar(255) DEFAULT NULL,
  `schedule_type` varchar(50) NOT NULL DEFAULT 'NONE',
  `schedule_conf` varchar(128) DEFAULT NULL,
  `misfire_strategy` varchar(50) NOT NULL DEFAULT 'DO_NOTHING',
  `executor_route_strategy` varchar(50) DEFAULT NULL,
  `executor_handler` varchar(255) DEFAULT NULL,
  `executor_param` varchar(512) DEFAULT NULL,
  `executor_block_strategy` varchar(50) DEFAULT NULL,
  `executor_timeout` int NOT NULL DEFAULT '0',
  `executor_fail_retry_count` int NOT NULL DEFAULT '0',
  `glue_type` varchar(50) NOT NULL,
  `glue_source` mediumtext,
  `glue_remark` varchar(128) DEFAULT NULL,
  `glue_updatetime` datetime DEFAULT NULL,
  `child_jobid` varchar(255) DEFAULT NULL,
  `trigger_status` tinyint NOT NULL DEFAULT '0',
  `trigger_last_time` bigint NOT NULL DEFAULT '0',
  `trigger_next_time` bigint NOT NULL DEFAULT '0',
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `xxl_job_log` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `job_group` int NOT NULL,
  `job_id` int NOT NULL,
  `executor_address` varchar(255) DEFAULT NULL,
  `executor_handler` varchar(255) DEFAULT NULL,
  `executor_param` varchar(512) DEFAULT NULL,
  `executor_sharding_param` varchar(20) DEFAULT NULL,
  `executor_fail_retry_count` int NOT NULL DEFAULT '0',
  `trigger_time` datetime DEFAULT NULL,
  `trigger_code` int NOT NULL,
  `trigger_msg` text,
  `handle_time` datetime DEFAULT NULL,
  `handle_code` int NOT NULL,
  `handle_msg` text,
  `alarm_status` tinyint NOT NULL DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `I_trigger_time` (`trigger_time`),
  KEY `I_handle_code` (`handle_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `xxl_job_log_report` (
  `id` int NOT NULL AUTO_INCREMENT,
  `trigger_day` datetime DEFAULT NULL,
  `running_count` int NOT NULL DEFAULT '0',
  `suc_count` int NOT NULL DEFAULT '0',
  `fail_count` int NOT NULL DEFAULT '0',
  `update_time` datetime DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `i_trigger_day` (`trigger_day`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `xxl_job_logglue` (
  `id` int NOT NULL AUTO_INCREMENT,
  `job_id` int NOT NULL,
  `glue_type` varchar(50) DEFAULT NULL,
  `glue_source` mediumtext,
  `glue_remark` varchar(128) NOT NULL,
  `add_time` datetime DEFAULT NULL,
  `update_time` datetime DEFAULT NULL,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `xxl_job_registry` (
  `id` int NOT NULL AUTO_INCREMENT,
  `registry_group` varchar(50) NOT NULL,
  `registry_key` varchar(255) NOT NULL,
  `registry_value` varchar(255) NOT NULL,
  `update_time` datetime DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `i_g_k_v` (`registry_group`,`registry_key`,`registry_value`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `xxl_job_user` (
  `id` int NOT NULL AUTO_INCREMENT,
  `username` varchar(50) NOT NULL,
  `password` varchar(50) NOT NULL,
  `role` tinyint NOT NULL,
  `permission` varchar(255) DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `i_username` (`username`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

INSERT INTO `xxl_job_user`(`id`, `username`, `password`, `role`, `permission`)
  VALUES (1, 'admin', 'e10adc3949ba59abbe56e057f20f883e', 1, NULL);

INSERT INTO `xxl_job_lock` (`lock_name`) VALUES ('schedule_lock');

exit;
```

重启 XXL-Job Admin（首次启动时数据库不存在会失败，初始化后重启）：

```bash
docker compose restart xxl-job-admin
```

等 20 秒后验证：访问 `http://<服务器IP>:8088/xxl-job-admin`，用 `admin/123456` 登录。

### 5.2 初始化 mhp 业务库

```bash
# schema.sql 包含所有 12 张业务表 + IK 词库初始数据，一条命令搞定
docker exec -i mhp-mysql mysql -uroot -p222333dyh \
  < /home/ubuntu/BookingSystem/BookSystem/mhp-app/src/main/resources/schema.sql
```

> `migration.sql` 是旧库升级补丁（ALTER TABLE），全新部署不需要执行。

---

## 六、构建并启动 Java 微服务

### 6.1 构建所有模块

```bash
cd /home/ubuntu/BookingSystem/BookSystem
mvn clean package -DskipTests
```

首次构建约 5~15 分钟（需要下载 Maven 依赖），之后会快很多。

构建成功后各 JAR 位置：
```
mhp-gateway/target/mhp-gateway-1.0.0.jar
mhp-account/target/mhp-account-1.0.0.jar
mhp-booking/target/mhp-booking-1.0.0.jar
mhp-social/target/mhp-social-1.0.0.jar
```

### 6.2 创建日志目录

```bash
mkdir -p /home/ubuntu/BookingSystem/logs
```

### 6.3 按顺序后台启动服务

服务器上用 `nohup` 后台运行，日志写入文件，SSH 断开后服务继续运行。

**必须按此顺序启动**（account 先，gateway 最后）：

```bash
cd /home/ubuntu/BookingSystem/BookSystem

# 1. 账号服务
nohup java -Xms128m -Xmx512m \
  -jar mhp-account/target/mhp-account-1.0.0.jar \
  > /home/ubuntu/BookingSystem/logs/account.log 2>&1 &

# 等待约 25 秒，确认启动
sleep 25 && tail -20 /home/ubuntu/BookingSystem/logs/account.log
# 应看到 "Started MhpAccountApplication"

# 2. 预约服务
nohup java -Xms128m -Xmx512m \
  -jar mhp-booking/target/mhp-booking-1.0.0.jar \
  > /home/ubuntu/BookingSystem/logs/booking.log 2>&1 &

# 3. 社区服务
nohup java -Xms128m -Xmx512m \
  -jar mhp-social/target/mhp-social-1.0.0.jar \
  > /home/ubuntu/BookingSystem/logs/social.log 2>&1 &

# 等待两个服务启动
sleep 25

# 4. 网关（最后启动）
nohup java -Xms128m -Xmx256m \
  -jar mhp-gateway/target/mhp-gateway-1.0.0.jar \
  > /home/ubuntu/BookingSystem/logs/gateway.log 2>&1 &
```

### 6.4 确认服务已注册到 Nacos

等待约 30 秒，访问 Nacos 控制台：
`http://<服务器IP>:8848/nacos` → 账号 nacos/nacos → 服务管理 → 服务列表

应看到 `mhp-account`、`mhp-booking`、`mhp-social` 三个服务。

### 6.5 查看服务日志

```bash
# 实时查看账号服务日志
tail -f /home/ubuntu/BookingSystem/logs/account.log

# 查看所有服务进程
ps aux | grep mhp
```

### 6.6 停止服务

```bash
# 一次性停止所有 Java 微服务
pkill -f "mhp-.*-1.0.0.jar"

# 停止单个服务（先查 PID）
ps aux | grep mhp
kill <PID>
```

---

## 七、初始化 Elasticsearch 索引

mhp-account 启动后，执行一次全量数据同步到 ES：

```bash
curl -X POST http://localhost:8081/internal/merchant/es/init
```

验证索引是否创建成功：
```bash
curl http://localhost:9200/merchant/_count
# 返回 {"count": N, ...} 说明正常
```

> 之后商家/用户数据变更会由 Canal CDC 实时同步，无需手动再执行。

---

## 八、构建前端 & 配置 Nginx

### 8.1 构建前端静态文件

```bash
cd /home/ubuntu/BookingSystem/cosplay-frontend

# 安装依赖（首次）
npm install

# 生产构建
npm run build
```

构建完成后，静态文件在 `dist/` 目录下。

> **前端环境版本（服务器实际版本）：**
>
> | 组件 | 版本 |
> |------|------|
> | Node.js | v18.20.8 |
> | npm | 10.8.2 |
> | Vue | 3.5.42 |
> | Vite | 5.4.21 |
> | TypeScript | 5.9.3 |
> | vue-tsc | 3.3.11 |
> | @stomp/stompjs | 7.3.0 |
> | sockjs-client | 1.6.1 |
>
> `vue-tsc` 使用 3.3.11（非旧版 1.x），兼容 TypeScript 5.9.3。若升级 Node.js 后遇到 `npm install` 报 peer dependency 警告，用 `npm install --legacy-peer-deps` 即可。

### 8.2 配置 Nginx

```bash
sudo tee /etc/nginx/conf.d/mhp.conf << 'EOF'
server {
    listen 80;
    server_name _;

    root /home/ubuntu/BookingSystem/cosplay-frontend/dist;
    index index.html;

    # Vue Router history 模式
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 请求转发给网关
    location /api/ {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # WebSocket 请求转发
    location /ws/ {
        proxy_pass http://localhost:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 3600s;
    }
}
EOF
```

### 8.3 检查并重启 Nginx

```bash
sudo nginx -t
sudo systemctl restart nginx
```

---

## 九、注册定时任务

访问 `http://<服务器IP>:8088/xxl-job-admin`，用 `admin/123456` 登录。

**第一步：确认执行器已注册**

进入「执行器管理」，应看到 `booking-executor` 已自动注册（mhp-booking 启动时上报）。
如果没有，点「新增」手动添加：AppName 填 `booking-executor`，注册方式选「自动注册」。

**第二步：新增定时任务**

进入「任务管理」→「新增」，创建两个任务：

**任务 1：超时取消预约**
```
执行器：booking-executor
任务描述：超时取消预约
运行模式：BEAN
JobHandler：cancelTimeoutBookingJob
Cron：0 */5 * * * ?
```

**任务 2：档期前一天提醒**
```
执行器：booking-executor
任务描述：档期提醒
运行模式：BEAN
JobHandler：reminderJob
Cron：0 0 9 * * ?
```

两个任务创建后都点「操作」→「启动」激活。

---

## 十、开放服务器防火墙端口

在云控制台的**安全组**规则中，开放以下入站端口：

| 端口 | 用途 | 是否必须 |
|------|------|----------|
| 80 | Nginx（前端 + API 入口） | 必须 |
| 8848 | Nacos 控制台 | 可选（管理用） |
| 8088 | XXL-Job Admin | 可选（管理用） |
| 15672 | RabbitMQ 管理台 | 可选（管理用） |
| 9200 | Elasticsearch | 可选（调试用） |
| 5601 | Kibana | 可选（调试用） |

> 管理类端口建议只对你自己的 IP 开放，不要对 `0.0.0.0/0` 全开。

---

## 十一、验证全流程

### 11.1 健康检查

```bash
# ES 状态
curl http://localhost:9200/_cluster/health

# RabbitMQ（返回 JSON 说明正常）
curl -u admin:123456 http://localhost:15672/api/overview

# 网关是否正常（随便一个接口）
curl http://localhost:8080/api/user/health
```

### 11.2 从浏览器访问

打开 `http://<服务器公网IP>`，应看到前端登录页。

### 11.3 冒烟测试主链路

1. **注册** → `POST /api/user/register`
2. **登录** → `POST /api/user/login`，拿到 token
3. **搜索商家** → `GET /api/merchant/search?keyword=妆娘` → ES 搜索结果
4. **查看商家主页** → `GET /api/merchant/{id}` → Redis 缓存生效
5. **发起预约** → `POST /api/booking`
6. **商家确认** → `PUT /api/booking/{id}/confirm` → RabbitMQ 发通知
7. **WebSocket 通知** → 前端消息页应收到实时推送

---

## 十二、常见问题排查

### Q1: Java 服务启动报 `Connection refused` 连不上 MySQL/Redis
```bash
docker compose ps
docker exec mhp-redis redis-cli ping   # 应返回 PONG
```
MySQL 容器需要约 30 秒才变为 healthy，等待后再启动 Java 服务。

### Q2: Nacos 服务列表为空，服务没注册
```bash
tail -50 /home/ubuntu/BookingSystem/logs/account.log | grep ERROR
```
最常见原因是 Nacos 还没完全启动，重启对应 Java 服务即可。

### Q3: 前端访问 502 Bad Gateway
```bash
sudo tail -20 /var/log/nginx/error.log
ps aux | grep mhp-gateway
curl http://localhost:8080
```

### Q4: ES 搜索返回 `index_not_found_exception`
```bash
curl -X POST http://localhost:8081/internal/merchant/es/init
```

### Q5: 前端 WebSocket 断开 / 消息收不到
检查 Nginx 配置是否正确加载：`sudo nginx -t && sudo systemctl reload nginx`

### Q6: XXL-Job 执行器未注册
```bash
grep "xxl-job" /home/ubuntu/BookingSystem/logs/booking.log
```
应看到 `>>>>>>>>>>> xxl-job regist JobThread success`。

### Q7: 七牛云上传报错
填入 application.yaml 真实凭据后，需要重新打包并重启 mhp-social：
```bash
cd /home/ubuntu/BookingSystem/BookSystem
mvn package -pl mhp-social -DskipTests
pkill -f "mhp-social"
nohup java -Xms128m -Xmx512m -jar mhp-social/target/mhp-social-1.0.0.jar \
  > /home/ubuntu/BookingSystem/logs/social.log 2>&1 &
```

---

## 十三、重启服务（日常运维）

```bash
# 重启所有中间件
cd /home/ubuntu/BookingSystem
docker compose restart

# 重启单个 Java 服务（以 account 为例）
pkill -f "mhp-account"
sleep 3
cd /home/ubuntu/BookingSystem/BookSystem
nohup java -Xms128m -Xmx512m \
  -jar mhp-account/target/mhp-account-1.0.0.jar \
  > /home/ubuntu/BookingSystem/logs/account.log 2>&1 &
```

---

## 十四、端口速查

```
80    → Nginx（前端 + API 唯一公网入口）
8080  → mhp-gateway（Nginx 内部转发目标，不对外）
8081  → mhp-account
8082  → mhp-booking
8083  → mhp-social
8084  → mhp-ai（Docker）
3306  → MySQL（Docker）
6379  → Redis（Docker）
5672  → RabbitMQ（Docker）
15672 → RabbitMQ 管理台
8848  → Nacos
8088  → XXL-Job Admin（容器内 8080 → 宿主 8088）
9200  → Elasticsearch
5601  → Kibana
11111 → Canal
9998  → XXL-Job Executor（mhp-booking 绑定）
```

---

## 附：代码已修复的问题

部署前已对以下文件做了修复，无需手动操作：

| 文件 | 修改内容 | 原因 |
|------|----------|------|
| `mhp-booking/application.yaml` | XXL-Job 地址 `8080` → `8088` | docker-compose 宿主机端口是 8088 |
| `mhp-gateway/application.yml` | 服务端口 `80` → `8080` | 避免与 Nginx 冲突 |
| `cosplay-frontend/vite.config.ts` | 代理目标 `:80` → `:8080` | 与网关端口保持一致 |
| `docker-compose.yml` | elasticsearch 服务加 `extra_hosts` | Linux 上 IK 词典热更新需要解析 `host.docker.internal` |
| `cosplay-frontend/src/views/ScheduleManageView.vue` | 图例 `<span class="dot" .../>` → `<span ...></span>` （4 处） | Vue 3.5 编译器不接受非 void 元素的自闭合写法，导致 `Cannot read properties of undefined (reading 'type')` 构建失败 |
| `cosplay-frontend/src/views/MerchantDetailView.vue` | 图例 `<span class="legend-dot" .../>` → `<span ...></span>` （2 处） | 同上 |
