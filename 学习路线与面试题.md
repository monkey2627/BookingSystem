# 档期平台开发学习路线 + 面试题详解

> **你的环境确认：**
> - 本地电脑：Windows，用于写代码
> - 服务器：Linux 4GB+，国内，已有，用于部署
> - 图片存储：七牛云（免费 10GB）
> - 域名：需购买（约 ¥55/年），需 ICP 备案
> - 年均成本：¥55（仅域名）

---

## 调研结论与功能更新（N=14：商家8人，客人6人）

### 核心发现

**商家端关键数据：**
- 8/8 人目前用手机备忘录管理档期 → 说明痛点是真实的，迁移门槛低
- 6/8 最想要可视化日历 → 档期日历是核心功能，必须做好
- 8/8 放弃平台的首要原因是"客人不够多" → 冷启动是最大风险，平台需要先让商家看到客人在用
- 抢档期模式：仅1人强烈支持，4人想保留私信选项 → **必须同时支持"直接选档期"和"抢档期"两种模式**
- 发现新服务类型：**后勤**（4人）、**后期**（3人）原设计未覆盖

**客人端关键数据：**
- 6/6 最想要的功能是"直接查看空闲档期" → 日历可见性是第一优先级
- 4/6 不知道去哪找合适的商家 → **搜索/发现功能不可省略**
- 5/6 用微信沟通，但微信在平台外 → **站内沟通需求真实存在**
- 3/6 遇到过定金问题 → 投诉/评价系统有必要

---

### 新增功能清单（调研驱动）

| 功能 | 需求来源 | 个人开发者可行性 | 优先级 |
|------|---------|----------------|--------|
| **评价系统**（订单完成后评分+文字+图片）| 客人2人明确提出 | ✅ 标准CRUD，完全可行 | 高 |
| **商家社交媒体链接**（小红书/微博跳转）| 商家1人提出 | ✅ 加几个文本字段，极简单 | 高（成本极低）|
| **扩展服务类型**（加后勤、后期） | 商家4人涉及 | ✅ 枚举扩展，极简单 | 高 |
| **用户搜索/发现**（按城市、类型筛选）| 客人4人反映找不到商家 | ✅ MySQL LIKE 基础版完全够 | 高 |
| **站内私信**（商家↔客人直接沟通）| 客人2人在Q7选择、商家提到保留私信约单 | ✅ WebSocket实现，可行 | 中 |
| **妆前问卷**（商家创建，客人约单时填写）| 商家1人提出 | ✅ 动态表单，可行，略复杂 | 中 |
| **投诉功能**（双方均可提交）| 商家+客人各提出 | ✅ 基础版可行，需人工审核 | 中 |
| **客人资料完善**（真实年龄/性别）| 商家1人提出 | ✅ user表加字段，极简单 | 中 |
| **下单后日历提醒** | 客人2人提出 | ✅ 已在XXL-Job提醒任务中覆盖 | 已覆盖 |
| **黑名单/信誉系统** | 商家1人提出 | ⚠️ 自动黑名单有被滥用风险，建议只做投诉记录+人工处理 | 低 |
| **按地理位置推荐** | 客人1人提出 | ⚠️ 基础版（按城市）✅；真正LBS复杂，暂不做 | 低 |
| **自动收款确认** | 商家1人提出 | ❌ 需要接入支付API，个人无法申请，维持跳转方案 | 不做 |

---

### 因调研需要调整的原设计

1. **服务类型枚举扩展**：妆娘/摄影/毛娘 → 妆娘/摄影/毛娘/后勤/后期/其他
2. **约单流程增加"直接选档期"入口**：不强制走抢档期，商家可以设置档期为"可直接预约"
3. **merchant 表新增字段**：小红书链接、微博链接、微信公众号等社交媒体入口
4. **user 表新增字段**：性别、生日（年龄从生日计算），约单时展示给商家

---

## 准备工作（开始之前，花1天）

### 本地电脑需要安装的软件

**第一步：申请 JetBrains 学生授权（先做，审核要1-2天）**
- 地址：jetbrains.com/student
- 用学校邮箱或学信网认证
- 通过后免费使用 IntelliJ IDEA Ultimate + DataGrip

**第二步：安装以下软件**

| 软件 | 用途 | 说明 |
|------|------|------|
| JDK 17 | Java 运行环境 | 搜"Eclipse Temurin 17"下载 |
| IntelliJ IDEA Ultimate | 写后端代码 | 等学生授权通过后安装 |
| Docker Desktop | 本地跑中间件 | 需要开启 WSL2，按提示操作 |
| Git | 版本管理 | git-scm.com |
| VS Code | 写前端代码 | 前端阶段再装不急 |
| Node.js 20 LTS | Vue 3 环境 | 前端阶段再装 |
| Apifox | 接口测试 | 替代 Postman，中文免费 |
| Another Redis Desktop Manager | 查看 Redis 数据 | 免费 |
| DataGrip | 查看数据库 | 学生免费 |

### 服务器需要做的事（10分钟）

SSH 连上服务器，安装 Docker：

**Ubuntu：**
```bash
curl -fsSL https://get.docker.com | sh
sudo usermod -aG docker $USER
sudo systemctl enable docker
# 退出重新登录后生效
```

**CentOS：**
```bash
sudo yum install -y yum-config-manager
sudo yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo
sudo yum install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin
sudo systemctl start docker && sudo systemctl enable docker
sudo usermod -aG docker $USER
```

验证：
```bash
docker --version
docker compose version
```

装完就不用管服务器了，等项目写好再回来部署。

### 域名和备案（现在买域名，备案等项目快完成时做）

```
现在：去阿里云/腾讯云买一个域名（和你的服务器同一家云商）
      约 ¥55/年，想好项目名字再买

项目快完成时：提交 ICP 备案
  → 实名认证 + 身份证照片 + 人脸识别
  → 等待审核：约 20 个工作日
  → 备案期间服务器不能对外提供服务，但本地开发不受影响
  → 备案通过后绑定域名正式上线
```

### 整体开发节奏

```
本地电脑（写代码）──git push──► GitHub/Gitee
                                      │
                               服务器 git pull
                               docker compose up
                                      │
                               访问服务器IP测试
```

---

## 补充：API 设计规范（现在就做好，后续加 Android App 零改动）

> 后端只要是纯 REST API，未来加 Android App 只需要再写一个 Android 前端，后端代码完全不动。以下 6 条规范从第一个接口开始就要遵守。

### 规范一：接口必须是纯 REST，不能耦合网页逻辑

```java
// ✅ 正确：只返回数据，前端/App 自己决定怎么展示
@GetMapping("/merchant/{id}")
public Result<MerchantVO> getMerchant(@PathVariable Long id) { ... }

// ❌ 错误：返回页面跳转、HTML 片段（服务端渲染）
```

### 规范二：接口加版本号前缀

```java
// ✅ 从一开始就加
@RequestMapping("/api/v1/merchant")
```

### 规范三：图片全部用七牛云链接，不存本地路径

```java
// ✅ 正确：返回完整 CDN URL
merchant.setAvatar("https://cdn.your-domain.com/avatar/xxx.jpg");

// ❌ 错误：本地路径，App 访问不到
merchant.setAvatar("/static/upload/avatar/xxx.jpg");
```

### 规范四：Token 通过 Header 传，不用 Cookie

```yaml
# application.yml
sa-token:
  token-name: token
  is-read-cookie: false   # 关闭 Cookie，只用 Header
  token-style: uuid
```

### 规范五：Feed 流和列表接口用游标分页

```java
// ❌ 页码分页：App 下拉加载时新数据插入会导致重复
GET /api/v1/posts?page=2&size=10

// ✅ 游标分页：App 下拉加载必须用这个
GET /api/v1/posts?lastId=12345&size=10

// 实现：
LambdaQueryWrapper<Post> wrapper = new LambdaQueryWrapper<Post>()
    .lt(lastId != null, Post::getId, lastId)
    .orderByDesc(Post::getId)
    .last("LIMIT " + size);
```

### 规范六：错误码语义化

```java
public enum ResultCode {
    SUCCESS(200, "成功"),
    UNAUTHORIZED(401, "未登录"),
    FORBIDDEN(403, "无权限"),
    SCHEDULE_FULL(10001, "档期已满"),
    SCHEDULE_NOT_OPEN(10002, "抢购尚未开始"),
    ORDER_DUPLICATE(10003, "您已预约该档期");
}
```

---

## 第一阶段：Docker（第1-2天）

### 要做什么

1. 本地安装 Docker Desktop，确保能正常启动
2. 在项目根目录创建 `docker-compose.yml`，一键启动所有中间件
3. 用 DataGrip 连接 MySQL，用 Another Redis Desktop Manager 连接 Redis，确认都能连上
4. 在服务器上也把 Docker 装好（见准备工作）

### 要学什么

**核心概念：**
- 镜像（Image）= 模板，容器（Container）= 运行的实例，就像类和对象的关系
- 数据卷（Volume）= 持久化存储，容器删了数据还在
- 端口映射 `3306:3306` = 宿主机端口:容器端口
- 同一个 docker-compose.yml 里的容器，直接用服务名互相访问（如 `mysql:3306`）

**本地开发用的 docker-compose.yml：**
```yaml
version: '3.8'
services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: 123456
      MYSQL_DATABASE: cosplay_schedule
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  redis:
    image: redis:7.0
    ports:
      - "6379:6379"
    command: redis-server --requirepass 123456

  rabbitmq:
    image: rabbitmq:3.12-management
    ports:
      - "5672:5672"
      - "15672:15672"   # 浏览器访问 localhost:15672 进管理界面
    environment:
      RABBITMQ_DEFAULT_USER: admin
      RABBITMQ_DEFAULT_PASS: 123456

  xxl-job-admin:
    image: xuxueli/xxl-job-admin:2.4.0
    ports:
      - "8080:8080"
    environment:
      PARAMS: "--spring.datasource.url=jdbc:mysql://mysql:3306/xxl_job?useUnicode=true&characterEncoding=UTF-8 --spring.datasource.username=root --spring.datasource.password=123456"
    depends_on:
      - mysql

volumes:
  mysql_data:
```

**常用命令：**
```bash
docker compose up -d        # 后台启动所有服务
docker compose down         # 停止并删除容器（数据卷保留）
docker compose logs -f      # 查看日志
docker compose ps           # 查看运行状态
docker exec -it mysql bash  # 进入容器内部
```

### 学习资料

| 资源 | 说明 | 获取方式 |
|------|------|----------|
| Docker 官方入门教程 | 最权威，30 分钟走通核心概念 | 搜索：`docker docs get started` |
| B站 Docker 快速入门 | 中文讲解，适合零基础 | B站搜索：`黑马程序员 Docker 2024` |
| Docker Compose 语法文档 | compose.yml 所有字段的参数说明 | 搜索：`docker compose file reference v3` |

**关键词速查**：`docker image container volume port-mapping network docker-compose up down logs`

### 验收标准

- `docker compose up -d` 后，四个服务全部 running
- DataGrip 能连上 MySQL
- Another Redis Desktop Manager 能连上 Redis
- 浏览器打开 `localhost:15672` 能看到 RabbitMQ 管理界面
- 浏览器打开 `localhost:8080/xxl-job-admin` 能看到 XXL-Job 界面

### 面试题

**Q：Docker 和虚拟机的区别？**

A：虚拟机需要模拟完整的硬件和操作系统，资源占用大、启动慢（分钟级）。Docker 容器共享宿主机内核，只隔离进程，资源占用小、启动快（秒级）。但虚拟机隔离性更强，容器共享内核存在安全风险。

---

## 第二阶段：Spring Boot 3（第3-10天）

### 要做什么

1. 在 IDEA 里用 Spring Initializr 创建项目（选 Spring Boot 3.2.x，Java 17）
2. 引入 MyBatis-Plus、Sa-Token、Redis、RabbitMQ、Sentinel 的 starter 依赖
3. 写好项目基础框架：统一返回结果、全局异常处理、参数校验
4. 写第一个接口：`GET /api/v1/hello` 返回 `{"code":200,"message":"成功","data":"hello"}`，用 Apifox 测试通过

### 要学什么

**核心注解：**
```java
@SpringBootApplication     // 启动类注解
@RestController            // = @Controller + @ResponseBody
@RequestMapping("/api/v1") // 路由前缀
@GetMapping / @PostMapping // 具体路由
@Service / @Repository     // 业务层/数据层
@Autowired                 // 依赖注入
@Value("${xxx}")           // 读取配置文件
@Transactional             // 事务
```

**必须写的基础框架（每个项目都要有）：**
```java
// 1. 统一返回结果
@Data
public class Result<T> {
    private Integer code;
    private String message;
    private T data;

    public static <T> Result<T> ok(T data) {
        Result<T> r = new Result<>();
        r.setCode(200);
        r.setMessage("成功");
        r.setData(data);
        return r;
    }

    public static Result<?> fail(String message) {
        Result<?> r = new Result<>();
        r.setCode(500);
        r.setMessage(message);
        return r;
    }
}

// 2. 自定义业务异常
public class BusinessException extends RuntimeException {
    private Integer code;
    public BusinessException(String message) {
        super(message);
        this.code = 500;
    }
    public BusinessException(ResultCode resultCode) {
        super(resultCode.getMessage());
        this.code = resultCode.getCode();
    }
}

// 3. 全局异常处理
@RestControllerAdvice
public class GlobalExceptionHandler {
    @ExceptionHandler(BusinessException.class)
    public Result<?> handleBusiness(BusinessException e) {
        return Result.fail(e.getCode(), e.getMessage());
    }

    @ExceptionHandler(MethodArgumentNotValidException.class)
    public Result<?> handleValidation(MethodArgumentNotValidException e) {
        String msg = e.getBindingResult().getFieldErrors()
            .stream().map(FieldError::getDefaultMessage)
            .collect(Collectors.joining(", "));
        return Result.fail(400, msg);
    }
}

// 4. 参数校验
public class CreateOrderDTO {
    @NotNull(message = "档期ID不能为空")
    private Long scheduleId;

    @NotBlank(message = "备注不能为空")
    @Length(max = 200, message = "备注不能超过200字")
    private String remark;
}
```

**pom.xml 核心依赖：**
```xml
<!-- Spring Boot -->
<parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.2.0</version>
</parent>

<dependencies>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <!-- MyBatis-Plus -->
    <dependency>
        <groupId>com.baomidou</groupId>
        <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
        <version>3.5.5</version>
    </dependency>
    <!-- MySQL -->
    <dependency>
        <groupId>com.mysql</groupId>
        <artifactId>mysql-connector-j</artifactId>
    </dependency>
    <!-- Redis -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-data-redis</artifactId>
    </dependency>
    <!-- Sa-Token -->
    <dependency>
        <groupId>cn.dev33</groupId>
        <artifactId>sa-token-spring-boot3-starter</artifactId>
        <version>1.38.0</version>
    </dependency>
    <!-- RabbitMQ -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-amqp</artifactId>
    </dependency>
    <!-- 参数校验 -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <!-- Lombok -->
    <dependency>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok</artifactId>
        <optional>true</optional>
    </dependency>
    <!-- Hutool 工具库 -->
    <dependency>
        <groupId>cn.hutool</groupId>
        <artifactId>hutool-all</artifactId>
        <version>5.8.25</version>
    </dependency>
    <!-- Swagger 文档 -->
    <dependency>
        <groupId>org.springdoc</groupId>
        <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        <version>2.3.0</version>
    </dependency>
</dependencies>
```

### 学习资料

| 资源 | 说明 | 获取方式 |
|------|------|----------|
| Spring Boot 官方文档 | 自动配置原理、所有配置属性参考 | 搜索：`spring boot 3.2 reference documentation` |
| 尚硅谷 Spring Boot 3 | 国内最完整的中文教学视频，从零到项目 | B站搜索：`尚硅谷 SpringBoot3` |
| Spring Initializr | 一键生成项目骨架，选好依赖直接下载 | 直接访问 start.spring.io |
| Lombok 官方文档 | @Data @Builder @Slf4j 等注解详解 | 搜索：`projectlombok features` |

**关键词速查**：`Spring Boot 自动配置 @EnableAutoConfiguration AOP 动态代理 @Transactional 失效 Bean 生命周期 @PostConstruct`

### 验收标准

- 项目能正常启动，无报错
- `GET /api/v1/hello` 返回正确的 JSON 格式
- 故意传错误参数，能收到参数校验的错误提示
- 访问 `localhost:8080/swagger-ui.html` 能看到 API 文档

### 面试题

**Q：Spring Boot 自动配置原理是什么？**

A：`@SpringBootApplication` 中的 `@EnableAutoConfiguration` 触发自动配置。它通过 `SpringFactoriesLoader` 读取所有 jar 包下 `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` 文件，得到所有自动配置类的全限定名。每个自动配置类上有 `@ConditionalOnClass`、`@ConditionalOnMissingBean` 等条件注解，只有满足条件才会生效。比如引入了 Redis Starter，`RedisAutoConfiguration` 就会自动创建 `RedisTemplate` Bean，开发者不需要手动配置。

**Q：@Transactional 的原理是什么？什么情况下会失效？**

A：原理：Spring 通过 AOP 动态代理为带 `@Transactional` 的方法生成代理对象，方法调用前开启事务，正常结束提交，抛出异常回滚。

失效场景：
1. **方法不是 public**：AOP 代理无法拦截非 public 方法
2. **类内部调用**：`this.methodB()` 绕过了代理对象，事务不生效
3. **异常被 catch 吞掉**：事务感知不到异常，不会回滚
4. **异常类型不对**：默认只回滚 `RuntimeException`，需指定 `rollbackFor = Exception.class`
5. **数据库不支持事务**：MyISAM 引擎不支持，要用 InnoDB

**Q：Spring Bean 的生命周期？**

A：实例化 → 属性注入 → Aware 接口回调 → `BeanPostProcessor.before()` → `@PostConstruct` → `BeanPostProcessor.after()`（AOP 代理在这里生成）→ Bean 可用 → 容器关闭时 `@PreDestroy`

**Q：AOP 的 JDK 动态代理和 CGLIB 的区别？**

A：JDK 动态代理要求目标类实现接口，通过反射生成代理类。CGLIB 通过继承目标类生成子类，不需要接口。Spring Boot 默认用 CGLIB，因为不需要强制写接口。

---

## 第三阶段：MyBatis-Plus（第5-10天，与Spring Boot并行）

### 要做什么

1. 设计并创建数据库表（这是整个项目最重要的基础，见下方索引设计）
2. 用 MyBatis-Plus Generator 或手动生成 Entity、Mapper、Service
3. 实现第一个完整的 CRUD 模块：商家信息（增删改查 + 分页）
4. 用 Apifox 测试所有接口

### 要学什么

```java
// 基础用法
@Mapper
public interface MerchantMapper extends BaseMapper<Merchant> {
    // 内置了 insert/deleteById/updateById/selectById 等方法
}

// LambdaQueryWrapper（推荐，防止字段名写错）
List<Order> orders = orderMapper.selectList(
    new LambdaQueryWrapper<Order>()
        .eq(Order::getMerchantId, merchantId)
        .eq(Order::getStatus, OrderStatus.CONFIRMED)
        .orderByDesc(Order::getCreateTime)
);

// 页码分页（用于有总数需求的场景，如搜索）
Page<Merchant> page = new Page<>(pageNum, pageSize);
Page<Merchant> result = merchantMapper.selectPage(page,
    new LambdaQueryWrapper<Merchant>()
        .like(Merchant::getCity, city)
);

// 游标分页（用于 Feed 流、订单列表等下拉加载场景）
List<Post> posts = postMapper.selectList(
    new LambdaQueryWrapper<Post>()
        .lt(lastId != null, Post::getId, lastId)
        .orderByDesc(Post::getId)
        .last("LIMIT " + size)
);

// 自动填充（不用每次手动设置时间）
@TableField(fill = FieldFill.INSERT)
private LocalDateTime createTime;

@TableField(fill = FieldFill.INSERT_UPDATE)
private LocalDateTime updateTime;
```

**数据库表和索引设计（含调研新增）：**
```sql
-- 用户表（新增：gender, birthday，商家需要了解客人基本信息）
CREATE TABLE user (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    phone VARCHAR(11) NOT NULL,
    nickname VARCHAR(50),
    avatar VARCHAR(500),
    role TINYINT NOT NULL COMMENT '1:客人 2:商家',
    gender TINYINT COMMENT '0:未填 1:男 2:女 3:其他',
    birthday DATE COMMENT '生日（用于计算年龄，不直接存年龄）',
    create_time DATETIME,
    update_time DATETIME,
    is_deleted TINYINT DEFAULT 0,
    UNIQUE KEY uk_phone (phone)
);

-- 商家信息表（新增：多种服务类型、社交媒体链接）
CREATE TABLE merchant (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    -- 调研发现有后勤(4人)、后期(3人)，原设计只有妆娘/摄影/毛娘，需要扩展
    service_types VARCHAR(50) COMMENT 'JSON数组: [1,2,3,4,5] 1妆娘2摄影3毛娘4后勤5后期',
    city VARCHAR(50),
    intro TEXT,
    alipay_link VARCHAR(500) COMMENT '支付宝收款链接',
    xianyu_link VARCHAR(500) COMMENT '闲鱼商品链接',
    -- 调研新增：商家希望能跳转到小红书展示例图
    xiaohongshu_link VARCHAR(500) COMMENT '小红书主页链接',
    weibo_link VARCHAR(500) COMMENT '微博主页链接',
    avg_score DECIMAL(2,1) DEFAULT 0 COMMENT '评价均分（冗余字段，避免每次计算）',
    review_count INT DEFAULT 0 COMMENT '评价总数',
    create_time DATETIME,
    update_time DATETIME,
    UNIQUE KEY uk_user_id (user_id),
    KEY idx_city_service (city, service_types(10))  -- 按城市+类型搜索
);

-- 档期表（新增：book_type 区分"抢档期"和"直接预约"两种模式）
-- 调研发现：仅1/8商家强烈支持纯抢档期，4/8希望保留私信/直接约模式
CREATE TABLE schedule (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    merchant_id BIGINT NOT NULL,
    date DATE NOT NULL,
    time_slot VARCHAR(20) COMMENT '如 10:00-12:00',
    status TINYINT COMMENT '0:空闲 1:已预约 2:不可用',
    book_type TINYINT DEFAULT 0 COMMENT '0:直接预约 1:抢档期模式',
    rush_open_time DATETIME COMMENT '抢档期开始时间（book_type=1时有效）',
    max_queue_size INT DEFAULT 10,
    create_time DATETIME,
    update_time DATETIME,
    KEY idx_merchant_date_status (merchant_id, date, status)
);

-- 订单表（新增：questionnaire_answer 存妆前问卷回答）
CREATE TABLE `order` (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    order_no VARCHAR(32) NOT NULL,
    user_id BIGINT NOT NULL,
    merchant_id BIGINT NOT NULL,
    schedule_id BIGINT NOT NULL,
    status TINYINT COMMENT '0:待确认 1:待付款 2:已定档 3:已完成 4:已取消',
    remark TEXT,
    questionnaire_answer JSON COMMENT '妆前问卷回答，格式: {"q1":"答案1","q2":"答案2"}',
    create_time DATETIME,
    update_time DATETIME,
    UNIQUE KEY uk_order_no (order_no),
    KEY idx_user_status (user_id, status),
    KEY idx_merchant_status (merchant_id, status),
    KEY idx_create_time (create_time)
);

-- 抢档期记录表
CREATE TABLE rush_record (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    schedule_id BIGINT NOT NULL,
    user_id BIGINT NOT NULL,
    rank_no INT,
    status TINYINT COMMENT '0:排队中 1:已联系 2:已转化 3:已放弃',
    rush_time DATETIME,
    KEY idx_schedule_time (schedule_id, rush_time)
);

-- 帖子表
CREATE TABLE post (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    merchant_id BIGINT NOT NULL,
    content TEXT,
    images VARCHAR(2000) COMMENT 'JSON数组，存多张图片URL',
    like_count INT DEFAULT 0,
    create_time DATETIME,
    update_time DATETIME,
    is_deleted TINYINT DEFAULT 0,
    KEY idx_merchant_create (merchant_id, create_time)
);

-- ★ 新增：评价表（调研：客人2人明确要求，是高优先级功能）
CREATE TABLE review (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    order_id BIGINT NOT NULL,
    user_id BIGINT NOT NULL COMMENT '评价人（客人）',
    merchant_id BIGINT NOT NULL COMMENT '被评价商家',
    score TINYINT NOT NULL COMMENT '1-5星',
    content VARCHAR(500),
    images VARCHAR(1000) COMMENT '晒图，JSON数组',
    reply VARCHAR(500) COMMENT '商家回复',
    create_time DATETIME,
    UNIQUE KEY uk_order_id (order_id),  -- 一个订单只能评价一次
    KEY idx_merchant_score (merchant_id, score)  -- 查商家评价列表
);

-- ★ 新增：投诉表（调研：商家+客人各自提出，基础版人工审核）
CREATE TABLE complaint (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    order_id BIGINT NOT NULL,
    complainant_id BIGINT NOT NULL COMMENT '投诉人',
    respondent_id BIGINT NOT NULL COMMENT '被投诉人',
    reason VARCHAR(500) NOT NULL,
    evidence VARCHAR(1000) COMMENT '截图等证据，JSON数组',
    status TINYINT DEFAULT 0 COMMENT '0:待处理 1:处理中 2:已处理',
    admin_reply VARCHAR(500),
    create_time DATETIME,
    KEY idx_respondent (respondent_id),
    KEY idx_status (status)
);

-- ★ 新增：站内私信表（调研：客人2人在Q7中选择"和商家在平台上直接沟通"）
CREATE TABLE message (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    from_user_id BIGINT NOT NULL,
    to_user_id BIGINT NOT NULL,
    content VARCHAR(1000) NOT NULL,
    msg_type TINYINT DEFAULT 0 COMMENT '0:文字 1:图片',
    is_read TINYINT DEFAULT 0,
    create_time DATETIME,
    KEY idx_conversation (from_user_id, to_user_id, create_time),
    KEY idx_to_user_unread (to_user_id, is_read)
);

-- ★ 新增：妆前问卷模板表（调研：商家1人明确提出，能让客人提前填写信息）
CREATE TABLE questionnaire_template (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    merchant_id BIGINT NOT NULL,
    title VARCHAR(100) NOT NULL,
    questions JSON NOT NULL COMMENT '题目列表: [{"id":1,"title":"肤质","type":"radio","options":["干皮","油皮","混合"]}]',
    is_required TINYINT DEFAULT 0 COMMENT '约单时是否必填',
    create_time DATETIME,
    update_time DATETIME,
    KEY idx_merchant (merchant_id)
);
```

### 学习资料

| 资源 | 说明 | 获取方式 |
|------|------|----------|
| MyBatis-Plus 官方文档 | 条件构造器 / 分页 / 代码生成完整教程 | 搜索：`mybatis-plus 官方文档` 或访问 baomidou.com |
| B站 MyBatisPlus 实战 | 中文视频，包含分页插件配置和 Generator | B站搜索：`黑马程序员 MyBatisPlus` |

**关键词速查**：`LambdaQueryWrapper BaseMapper IPage PaginationInnerInterceptor #{} 与 ${} 区别 SQL注入`

### 验收标准

- 所有表创建成功，DataGrip 中能看到表结构
- 商家信息 CRUD 接口全部可用
- 分页查询返回正确的 total 和 records
- EXPLAIN 查看档期表查询，确认走了 `idx_merchant_date_status` 索引

### 面试题

**Q：MyBatis 中 #{} 和 ${} 的区别？**

A：`#{}` 是预编译参数，底层用 PreparedStatement 的 `?` 占位符，会对参数转义，**能防止 SQL 注入**，是推荐方式。`${}` 是字符串直接拼接，存在 SQL 注入风险，只在动态表名、列名等无法用 `#{}` 的场景使用。

**Q：MyBatis-Plus 的分页是如何实现的？**

A：通过 MyBatis 拦截器在 SQL 执行前拦截，自动追加 `LIMIT ?, ?`，并额外执行 `SELECT COUNT(*)` 获取总条数。需要注册 `MybatisPlusInterceptor` 并添加 `PaginationInnerInterceptor`。

> ⚠️ **App 兼容提醒**：Feed 流和订单列表用游标分页，商家搜索列表可用页码分页。

---

## 第四阶段：MySQL 索引（第8-12天，与上阶段并行）

### 要做什么

1. 用 EXPLAIN 分析项目中每一条查询语句，确认都走了正确的索引
2. 能手画出 B+ 树的结构图（面试用）
3. 理解项目中每个索引的设计原因，能在面试中说清楚

### 要学什么

**B+ 树结构（必须能画出来）：**
```
非叶子节点只存索引键值，不存数据行 → 每个节点能放更多索引，树更矮
叶子节点存完整数据（聚簇索引）或主键值（非聚簇索引）
叶子节点之间有双向链表 → 支持范围查询不用回到根节点
```

**项目中每个索引的设计理由：**
```
schedule 表：(merchant_id, date, status)
  → 最常见查询：WHERE merchant_id=? AND date BETWEEN ? AND ?
  → merchant_id 等值匹配，date 范围查询，符合最左前缀
  → status 在索引里，查询时不需要回表（覆盖索引）

order 表：(user_id, status) 和 (merchant_id, status)
  → 客人查"我的订单"用第一个，商家查"收到的订单"用第二个
  → 两个查询方向都需要，所以建两个索引

order 表：(create_time)
  → XXL-Job 超时扫描：WHERE status=? AND create_time < ?
  → 单独给 create_time 建索引，避免全表扫描

post 表：(merchant_id, create_time)
  → 查商家发布的帖子，按时间倒序：WHERE merchant_id=? ORDER BY create_time DESC
```

### 学习资料

| 资源 | 说明 | 获取方式 |
|------|------|----------|
| 小林 coding MySQL 系列 | B+树/索引/事务/MVCC 图文精讲，面试必看 | 搜索：`小林coding MySQL` 或访问 xiaolincoding.com |
| 极客时间《MySQL实战45讲》 | 丁奇（前阿里数据库专家），系统深入 | 搜索：`极客时间 MySQL实战45讲`（付费） |
| EXPLAIN 字段完整解析 | 如何看懂 type/key/rows/Extra 等字段 | 掘金搜索：`MySQL EXPLAIN 详解` |

**关键词速查**：`B+树 聚簇索引 覆盖索引 EXPLAIN type range ref index 最左前缀 MVCC ReadView undo log`

### 面试题

**Q：为什么用 B+ 树而不是哈希表？**

A：哈希表查单条是 O(1)，但不支持范围查询（`BETWEEN`、`>`、`ORDER BY`），而实际 SQL 中范围查询很常见。B+ 树叶子节点有链表，范围查询找到起点后顺着链表走就行。

**Q：联合索引 (a, b, c)，哪些查询能走索引？**

A：遵循最左前缀原则：
- `WHERE a=1` ✅、`WHERE a=1 AND b=2` ✅、`WHERE a=1 AND b=2 AND c=3` ✅
- `WHERE b=2` ❌（跳过了 a）
- `WHERE a=1 AND c=3` ✅（a 走，c 失效）
- `WHERE a=1 AND b>2 AND c=3` ✅（a、b 走，b 是范围后 c 失效）

**Q：事务的四个隔离级别？**

A：
| 隔离级别 | 脏读 | 不可重复读 | 幻读 |
|---------|------|---------|------|
| READ UNCOMMITTED | 有 | 有 | 有 |
| READ COMMITTED | 无 | 有 | 有 |
| REPEATABLE READ（MySQL默认）| 无 | 无 | 部分解决 |
| SERIALIZABLE | 无 | 无 | 无 |

MySQL InnoDB 在 REPEATABLE READ 下通过 MVCC + Next-Key Lock 解决了大部分幻读场景。

**Q：MVCC 是什么原理？**

A：每行数据有两个隐藏字段：`trx_id`（最后修改的事务ID）和 `roll_pointer`（指向 undo log）。读操作时根据 ReadView 判断哪个版本可见，不需要加锁，读写互不阻塞。

---

## 第五阶段：Sa-Token 认证（第10-12天）

### 要做什么

1. 实现完整的注册/登录接口（手机号 + 密码，暂不做短信验证码）
2. 配置路由拦截器，未登录访问需要鉴权的接口返回 401
3. 区分商家角色和客人角色，商家才能操作的接口加角色校验
4. 用 Apifox 测试：未登录请求报 401，登录后带 token 请求成功

### 要学什么

```java
// application.yml
sa-token:
  token-name: token
  timeout: 2592000       # token 有效期 30 天（秒）
  is-concurrent: true    # 允许同一账号多端登录
  is-read-cookie: false  # 关闭 Cookie，只用 Header（App 兼容）
  token-style: uuid

// 登录
StpUtil.login(userId);
String token = StpUtil.getTokenValue(); // 返回给前端

// 获取当前登录用户
Long userId = StpUtil.getLoginIdAsLong();

// 注销
StpUtil.logout();

// 角色校验
StpUtil.checkRole("merchant"); // 不是商家则抛异常

// 路由拦截器
@Configuration
public class SaTokenConfig implements WebMvcConfigurer {
    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        registry.addInterceptor(new SaInterceptor(handle -> {
            SaRouter.match("/**")
                    .notMatch("/api/v1/user/login", "/api/v1/user/register")
                    .check(r -> StpUtil.checkLogin());
        })).addPathPatterns("/**");
    }
}
```

### 学习资料

| 资源 | 说明 | 获取方式 |
|------|------|----------|
| Sa-Token 官方文档 | 登录 / 鉴权 / 路由拦截 / 多端登录完整教程 | 搜索：`sa-token 官方文档` 或访问 sa-token.cc |
| JWT.io | 在线解码 JWT，方便调试 Token 内容 | 直接访问 jwt.io |

**关键词速查**：`Sa-Token StpUtil login checkRole JWT Header.Payload.Signature 踢人下线 双token 路由拦截器`

### 验收标准

- 注册、登录接口正常工作
- 未带 token 访问 `/api/v1/merchant/info` 返回 401
- 登录后带 token 访问成功
- 用客人账号访问商家专属接口返回 403

### 面试题

**Q：JWT 的结构是什么？**

A：三部分用 `.` 分隔：Header（算法类型，Base64编码）、Payload（用户ID等声明，Base64编码，**不能存敏感信息**）、Signature（用密钥对前两部分签名，防篡改）。

**Q：JWT 如何踢人下线？**

A：纯 JWT 是无状态的，无法主动失效。Sa-Token 的做法是在 Redis 中存储 `用户ID → 版本号`，token 中携带版本号，每次验证时比对，退出登录时递增版本号，所有旧 token 立即失效。

**Q：Token 过期了怎么续签？**

A：双 token 方案：access_token 有效期短（30分钟）用于鉴权，refresh_token 有效期长（7天）用于换新 token。access_token 过期后前端用 refresh_token 请求换新，用户无感知。Sa-Token 也支持配置 `is-dynamic: true` 每次访问自动续期。

---

## 第六阶段：Redis（第13-20天，重点）

### 要做什么

1. 给商家主页接口加缓存，实现 Cache Aside 模式
2. 写出抢档期的 Lua 脚本，用单元测试验证并发场景下不会超卖
3. 实现缓存穿透保护（空值缓存）
4. 实现缓存击穿保护（Redisson 互斥锁重建）

### 要学什么

**五种数据结构及本项目用途：**
```
String  → 商家信息缓存（JSON）、验证码、标记"已处理"的消息ID
Hash    → Sa-Token 内部存 Session
Set     → 商家服务标签集合
ZSet    → 抢档期排队队列（score=时间戳）
```

**Cache Aside 模式实现：**
```java
public MerchantVO getMerchantInfo(Long merchantId) {
    String key = "merchant:info:" + merchantId;

    // 1. 查缓存
    String cached = stringRedisTemplate.opsForValue().get(key);

    // 2. 缓存穿透：查到空字符串说明数据库也没有
    if ("".equals(cached)) {
        throw new BusinessException("商家不存在");
    }

    // 3. 缓存命中
    if (cached != null) {
        return JSONUtil.toBean(cached, MerchantVO.class);
    }

    // 4. 缓存击穿：用分布式锁防止大量请求同时重建缓存
    RLock lock = redissonClient.getLock("lock:merchant:" + merchantId);
    try {
        lock.lock();
        // 双重检查（拿到锁后再查一次缓存，可能已被其他线程重建）
        cached = stringRedisTemplate.opsForValue().get(key);
        if (cached != null) {
            return JSONUtil.toBean(cached, MerchantVO.class);
        }

        // 5. 查数据库
        Merchant merchant = merchantMapper.selectById(merchantId);

        if (merchant == null) {
            // 缓存空值，防止穿透，TTL 设短
            stringRedisTemplate.opsForValue().set(key, "", 2, TimeUnit.MINUTES);
            throw new BusinessException("商家不存在");
        }

        MerchantVO vo = convertToVO(merchant);
        // 6. 写缓存，TTL 加随机偏移防雪崩
        int ttl = 30 + new Random().nextInt(10);
        stringRedisTemplate.opsForValue().set(key, JSONUtil.toJsonStr(vo), ttl, TimeUnit.MINUTES);
        return vo;
    } finally {
        lock.unlock();
    }
}

// 商家修改信息时删除缓存（Cache Aside 的更新策略）
public void updateMerchant(Long merchantId, UpdateMerchantDTO dto) {
    merchantMapper.updateById(convert(dto));
    stringRedisTemplate.delete("merchant:info:" + merchantId);
}
```

**抢档期 Lua 脚本：**
```lua
-- KEYS[1] = schedule:{scheduleId}
-- ARGV[1] = userId
-- ARGV[2] = 时间戳（score）
-- ARGV[3] = 最大排队人数

local key = KEYS[1]
local userId = ARGV[1]
local score = tonumber(ARGV[2])
local maxSize = tonumber(ARGV[3])

-- 判断是否已在队列中（防重复抢）
local existScore = redis.call('ZSCORE', key, userId)
if existScore then
    return -1
end

-- 判断队列是否已满
local currentSize = redis.call('ZCARD', key)
if currentSize >= maxSize then
    return 0
end

-- 加入队列，返回排名（第几位）
redis.call('ZADD', key, score, userId)
local rank = redis.call('ZRANK', key, userId)
return rank + 1
```

**Java 中调用 Lua 脚本：**
```java
private static final DefaultRedisScript<Long> RUSH_SCRIPT;
static {
    RUSH_SCRIPT = new DefaultRedisScript<>();
    RUSH_SCRIPT.setLocation(new ClassPathResource("lua/rush.lua"));
    RUSH_SCRIPT.setResultType(Long.class);
}

public long rush(Long scheduleId, Long userId, int maxSize) {
    Long result = stringRedisTemplate.execute(
        RUSH_SCRIPT,
        Collections.singletonList("schedule:" + scheduleId),
        userId.toString(),
        String.valueOf(System.currentTimeMillis()),
        String.valueOf(maxSize)
    );
    return result == null ? 0 : result;
    // -1: 已在队列，0: 队列已满，>0: 排名
}
```

### 学习资料

| 资源 | 说明 | 获取方式 |
|------|------|----------|
| 小林 coding Redis 系列 | 缓存三问 / 持久化 / 数据结构，图文精讲 | 搜索：`小林coding Redis` 或访问 xiaolincoding.com |
| Redis 官方命令参考 | 每个命令的参数、时间复杂度 | 搜索：`redis commands documentation` |
| B站 Redis7 实战教程 | 中文讲解，ZSet / Lua / RDB / AOF 都有 | B站搜索：`尚硅谷 Redis7` |
| Redis Lua 脚本教程 | EVAL 命令和 Lua 在 Redis 中的用法 | 搜索：`redis scripting documentation` |

**关键词速查**：`Redis ZSet Lua EVAL 缓存穿透 布隆过滤器 缓存击穿 互斥锁 缓存雪崩 TTL随机 RDB AOF`

### 验收标准

- 第一次请求商家主页查数据库，第二次命中缓存（用日志观察）
- 修改商家信息后，再次请求拿到最新数据（缓存已清除）
- 用 JMeter 模拟 100 个并发请求抢同一档期，队列不超过设定的最大人数
- Lua 脚本返回值正确（-1/0/正整数）

### 面试题

**Q：Redis 为什么快？**

A：纯内存操作；单线程处理命令（避免锁竞争，Redis 6.0 后 IO 多线程但命令执行仍单线程）；epoll 实现 IO 多路复用；高效数据结构（跳表支持 O(logN) 有序操作）。

**Q：缓存穿透、击穿、雪崩的区别和解决方案？**

A：
- **穿透**：查询不存在的数据，每次都打到 DB。解决：缓存空值（TTL设短）或布隆过滤器
- **击穿**：热点 key 过期瞬间，大量请求同时重建缓存。解决：互斥锁（只让一个请求重建，本项目用 Redisson）
- **雪崩**：大量 key 同时过期。解决：TTL 加随机偏移量（±5分钟），本项目已实现

**Q：为什么抢档期用 Lua 脚本而不是 Redis 事务（MULTI/EXEC）？**

A：Redis 事务的 MULTI/EXEC 不是原子的。MULTI 后如果使用了 WATCH，其他客户端修改了被 WATCH 的 key，EXEC 会返回 nil 而不是执行命令，需要客户端自己重试。而 Lua 脚本在 Redis 中是真正原子执行的，执行期间不会有其他命令插入，一次请求完成"判断 + 写入"两步，彻底防止并发问题。

**Q：ZSet 的底层数据结构？**

A：元素少时用 listpack（紧凑存储）；超过阈值后用跳表 + 哈希表。跳表支持 O(logN) 的按 score 范围查询，哈希表支持 O(1) 的按 member 查 score。本项目排队人数有限，实际用 listpack。

---

## 第七阶段：Redisson 分布式锁（第18-20天）

### 要做什么

1. 在订单创建接口加 Redisson 锁，防止同一用户重复提交
2. 在商家信息缓存重建时用 Redisson 锁（第六阶段已写）
3. 能解释 WatchDog 的工作原理

### 要学什么

```java
@Autowired
private RedissonClient redissonClient;

public Result<?> createOrder(Long scheduleId) {
    Long userId = StpUtil.getLoginIdAsLong();
    String lockKey = "order:create:" + userId + ":" + scheduleId;
    RLock lock = redissonClient.getLock(lockKey);

    try {
        // tryLock: 最多等3秒，持有锁30秒（WatchDog会自动续期）
        boolean locked = lock.tryLock(3, 30, TimeUnit.SECONDS);
        if (!locked) {
            return Result.fail("操作太频繁，请稍后再试");
        }

        // 查重：防止网络重试导致重复下单
        long count = orderMapper.selectCount(
            new LambdaQueryWrapper<Order>()
                .eq(Order::getUserId, userId)
                .eq(Order::getScheduleId, scheduleId)
                .ne(Order::getStatus, OrderStatus.CANCELLED)
        );
        if (count > 0) {
            return Result.fail("您已预约该档期");
        }

        // 创建订单
        Order order = new Order();
        order.setUserId(userId);
        order.setScheduleId(scheduleId);
        order.setStatus(OrderStatus.PENDING);
        order.setOrderNo(IdUtil.fastSimpleUUID());
        orderMapper.insert(order);

        return Result.ok("预约成功");
    } finally {
        if (lock.isHeldByCurrentThread()) {
            lock.unlock();
        }
    }
}
```

### 学习资料

| 资源 | 说明 | 获取方式 |
|------|------|----------|
| Redisson 官方 Wiki | 公平锁 / 读写锁 / 信号量等锁类型完整说明 | GitHub 搜索：`redisson` 进 wiki 页面 |
| 掘金 Redisson 分布式锁原理 | WatchDog 续期 + 可重入锁 Hash 结构图解 | 掘金搜索：`Redisson 分布式锁 WatchDog 原理` |

**关键词速查**：`Redisson WatchDog 续期 RLock tryLock lock 可重入锁 Hash Lua 原子性`

### 面试题

**Q：Redisson 的 WatchDog 机制是什么？**

A：使用 `lock.lock()` 不指定过期时间时，Redisson 启动后台线程，每隔 10 秒（lockWatchdogTimeout/3，默认30秒）检查锁是否仍被持有，是则续期到30秒。业务完成调用 `unlock()` 后 WatchDog 停止。解决了业务执行时间超过锁过期时间导致锁提前释放的问题。

**Q：Redisson 可重入锁的实现原理？**

A：锁存储在 Redis Hash 中：`key` 是锁名，`field` 是线程标识（UUID+线程ID），`value` 是重入次数。同一线程再次加锁时 value +1，释放时 value -1，减到0才删除 key。整个过程用 Lua 脚本保证原子性。

---

## 第八阶段：RabbitMQ（第20-26天）

### 要做什么

1. 配置交换机、队列、死信队列
2. 实现"抢档期成功 → 发消息 → 消费者发站内通知"完整链路
3. 实现消息幂等性（防重复消费）
4. 验证消费失败后消息进入死信队列

### 要学什么

```java
// 配置
@Configuration
public class RabbitMQConfig {
    @Bean
    public TopicExchange scheduleExchange() {
        return new TopicExchange("schedule.exchange", true, false);
    }

    @Bean
    public DirectExchange deadLetterExchange() {
        return new DirectExchange("schedule.dead.exchange", true, false);
    }

    @Bean
    public Queue notifyQueue() {
        return QueueBuilder.durable("notify.queue")
            .withArgument("x-dead-letter-exchange", "schedule.dead.exchange")
            .withArgument("x-dead-letter-routing-key", "dead.notify")
            .build();
    }

    @Bean
    public Binding notifyBinding() {
        return BindingBuilder.bind(notifyQueue())
            .to(scheduleExchange()).with("notify.#");
    }
}

// 生产者
rabbitTemplate.convertAndSend("schedule.exchange", "notify.rush", message);

// 消费者（手动 ACK + 幂等）
@RabbitListener(queues = "notify.queue")
public void handleNotify(NotifyMessage msg, Channel channel,
                          @Header(AmqpHeaders.DELIVERY_TAG) long tag) throws IOException {
    try {
        // 幂等性检查
        Boolean processed = stringRedisTemplate.opsForValue()
            .setIfAbsent("msg:processed:" + msg.getMsgId(), "1", 24, TimeUnit.HOURS);
        if (Boolean.FALSE.equals(processed)) {
            channel.basicAck(tag, false); // 已处理，直接 ACK
            return;
        }
        notifyService.send(msg);
        channel.basicAck(tag, false);
    } catch (Exception e) {
        channel.basicNack(tag, false, false); // 失败，进死信队列
    }
}
```

### 学习资料

| 资源 | 说明 | 获取方式 |
|------|------|----------|
| RabbitMQ 官方 Tutorial（6个）| 从 Hello World 到死信队列，必须做一遍 | 搜索：`rabbitmq tutorials` 进 rabbitmq.com |
| 尚硅谷 RabbitMQ | Confirm / 手动 ACK / 死信 / 延迟队列 | B站搜索：`尚硅谷 RabbitMQ` |
| 小林 coding 消息队列 | 消息可靠性 / 重复消费 / 顺序消费原理 | 搜索：`小林coding 消息队列` |

**关键词速查**：`RabbitMQ TopicExchange 死信队列DLX Confirm 手动ACK 幂等性 SET NX 消息持久化`

### 面试题

**Q：如何保证消息不丢失？**

A：三个环节都要处理：生产者开启 Confirm 模式（MQ 收到后回调，失败重发）；交换机、队列、消息都设置持久化；消费者关闭自动 ACK 改为手动 ACK，处理完再确认。

**Q：如何防止重复消费？**

A：每条消息携带唯一 msgId（UUID），消费时用 Redis SET NX 做原子性的"查+记录"，已处理的消息直接 ACK 跳过。

**Q：死信队列的触发条件？**

A：消息被 basicNack 且 requeue=false；消息超过 TTL；队列满了。本项目用死信队列兜底：通知失败的消息不丢失，人工或定时补偿处理。

---

## 第九阶段：XXL-Job（第24-26天）

### 要做什么

1. 在 docker-compose.yml 中启动 XXL-Job 调度中心（第一阶段已配置）
2. 在 Spring Boot 项目中配置执行器，连接调度中心
3. 实现订单超时取消任务，在 XXL-Job 控制台配置每分钟执行一次
4. 实现活动前日提醒任务，每天 9:00 执行

### 要学什么

```java
// application.yml 执行器配置
xxl:
  job:
    admin:
      addresses: http://localhost:8080/xxl-job-admin
    executor:
      appname: cosplay-executor
      port: 9999

// 订单超时取消
@XxlJob("cancelTimeoutOrderJob")
public void cancelTimeoutOrderJob() {
    LocalDateTime timeout = LocalDateTime.now().minusMinutes(30);
    List<Order> list = orderMapper.selectList(
        new LambdaQueryWrapper<Order>()
            .eq(Order::getStatus, OrderStatus.PENDING_PAYMENT)
            .lt(Order::getCreateTime, timeout)
            .last("LIMIT 100")  // 分批，防止一次处理太多
    );
    for (Order order : list) {
        orderService.cancel(order.getId(), "超时未付款自动取消");
        // 取消同时：更新订单状态 + 释放档期 + 发 MQ 通知双方
    }
    XxlJobHelper.log("本次处理超时订单: " + list.size() + " 条");
}

// 前日提醒
@XxlJob("reminderJob")
public void reminderJob() {
    LocalDate tomorrow = LocalDate.now().plusDays(1);
    List<Order> list = orderMapper.selectTomorrowOrders(tomorrow);
    for (Order order : list) {
        // 发站内通知给商家和客人
        rabbitTemplate.convertAndSend("schedule.exchange", "notify.reminder", order);
    }
}
```

### 学习资料

| 资源 | 说明 | 获取方式 |
|------|------|----------|
| XXL-Job 官方文档 | 执行器配置 / 任务类型 / 分片广播 / GLUE 模式 | GitHub 搜索：`xuxueli xxl-job` 看 README |
| B站 XXL-Job 快速接入 | 10~20 分钟从零接入 Spring Boot | B站搜索：`XXL-Job SpringBoot 快速接入` |

**关键词速查**：`XXL-Job @XxlJob 执行器 调度中心 appname Cron 分片广播 XxlJobHelper @Scheduled 区别`

### 面试题

**Q：为什么用 XXL-Job 而不是 @Scheduled？**

A：`@Scheduled` 是单机的，多实例部署时每个节点都会执行，同一订单会被取消多次。XXL-Job 是分布式调度平台，调度中心统一分配，同一时刻只有一个执行器节点执行任务。此外 XXL-Job 提供任务监控、失败重试、执行日志，`@Scheduled` 都没有。

**Q：XXL-Job 分片广播的使用场景？**

A：数据量大时用分片广播。多个节点同时执行，每个节点处理 `id % 总节点数 == 当前节点序号` 的数据，并行提升效率。比如 10 万条数据，3 个节点各处理约 3.3 万条。

---

## 第十阶段：Sentinel 限流（第26-28天）

### 要做什么

1. 对抢档期接口配置 QPS 限流规则（建议设 50-100）
2. 实现自定义降级返回（返回"人数过多请稍后再试"而不是报错）
3. 启动 Sentinel Dashboard，在控制台动态修改限流规则

### 要学什么

```java
@SentinelResource(
    value = "rushSchedule",
    blockHandler = "rushBlockHandler",
    fallback = "rushFallback"
)
@PostMapping("/api/v1/schedule/rush/{scheduleId}")
public Result<?> rushSchedule(@PathVariable Long scheduleId) {
    return scheduleService.rush(scheduleId, StpUtil.getLoginIdAsLong());
}

public Result<?> rushBlockHandler(Long scheduleId, BlockException e) {
    return Result.fail(429, "当前抢购人数过多，请稍后再试");
}

public Result<?> rushFallback(Long scheduleId, Throwable t) {
    return Result.fail("系统繁忙，请稍后再试");
}
```

### 学习资料

| 资源 | 说明 | 获取方式 |
|------|------|----------|
| Sentinel 官方 Wiki | 限流 / 熔断 / 降级规则配置详解 | GitHub 搜索：`alibaba sentinel` 进 wiki |
| B站 Sentinel 实战 | Spring Cloud Alibaba 系列中有独立章节 | B站搜索：`黑马程序员 SpringCloudAlibaba Sentinel` |

**关键词速查**：`Sentinel 滑动窗口 QPS限流 @SentinelResource blockHandler fallback 熔断降级 令牌桶 漏桶`

### 面试题

**Q：常见限流算法的区别？**

A：
- **固定窗口**：简单但有临界突刺（窗口边界瞬间2倍流量）
- **滑动窗口**：细分时间格，随时间滑动，解决临界问题，Sentinel 使用
- **漏桶**：固定速率输出，平滑流量，但突发流量全丢弃
- **令牌桶**：固定速率放令牌，允许一定突发（桶中积累令牌），Guava 使用

**Q：Sentinel 和 Hystrix 的区别？**

A：Sentinel 支持限流（Hystrix 不支持）；Sentinel 用信号量隔离，Hystrix 用线程池隔离；Sentinel Dashboard 支持规则动态配置；Hystrix 已停止维护，Sentinel 是当前主流。

---

## 第十一阶段：七牛云图片存储（第26-28天，与Sentinel并行）

### 要做什么

1. 注册七牛云账号，创建存储空间（Bucket）
2. 在 Spring Boot 中实现文件上传接口，上传后返回 CDN 链接
3. 前端图片全部通过这个接口上传，不走本地存储

### 要学什么

```xml
<!-- pom.xml -->
<dependency>
    <groupId>com.qiniu</groupId>
    <artifactId>qiniu-java-sdk</artifactId>
    <version>7.14.0</version>
</dependency>
```

```java
// 配置
@Configuration
@ConfigurationProperties(prefix = "qiniu")
@Data
public class QiniuConfig {
    private String accessKey;
    private String secretKey;
    private String bucket;
    private String domain;  // CDN 域名，如 cdn.your-domain.com
}

// 上传工具类
@Component
public class QiniuUtil {
    @Autowired
    private QiniuConfig config;

    public String upload(MultipartFile file) throws Exception {
        Auth auth = Auth.create(config.getAccessKey(), config.getSecretKey());
        String upToken = auth.uploadToken(config.getBucket());

        String fileName = IdUtil.fastSimpleUUID() +
            "." + FileUtil.extName(file.getOriginalFilename());

        UploadManager uploadManager = new UploadManager(new Configuration());
        Response response = uploadManager.put(
            file.getBytes(), fileName, upToken
        );

        if (!response.isOK()) {
            throw new BusinessException("图片上传失败");
        }
        // 返回完整 CDN URL
        return "https://" + config.getDomain() + "/" + fileName;
    }
}

// 上传接口
@PostMapping("/api/v1/upload")
public Result<String> upload(@RequestParam MultipartFile file) throws Exception {
    // 校验文件类型
    String ext = FileUtil.extName(file.getOriginalFilename());
    if (!Set.of("jpg", "jpeg", "png", "webp").contains(ext.toLowerCase())) {
        return Result.fail("只支持 jpg/png/webp 格式");
    }
    // 校验文件大小（5MB）
    if (file.getSize() > 5 * 1024 * 1024) {
        return Result.fail("图片不能超过5MB");
    }
    String url = qiniuUtil.upload(file);
    return Result.ok(url);
}
```

```yaml
# application.yml
qiniu:
  access-key: your-access-key
  secret-key: your-secret-key
  bucket: cosplay-images
  domain: cdn.your-domain.com
```

### 学习资料

| 资源 | 说明 | 获取方式 |
|------|------|----------|
| 七牛云 Java SDK 文档 | 上传凭证 / 覆盖上传 / CDN 配置 | 搜索：`七牛云 Java SDK 上传` 进 developer.qiniu.com |
| 七牛云对象存储控制台 | 创建 Bucket / 配置 CDN 加速域名 / 查看用量 | 登录 portal.qiniu.com |

**关键词速查**：`七牛云 Auth uploadToken UploadManager MultipartFile CDN 加速域名 bucket`

---

## 第十二阶段：Vue 3 前端（第28-42天）

### 要做什么

1. 用 Vite 创建 Vue 3 + TypeScript 项目
2. 封装 Axios（统一加 token、统一处理错误码）
3. 实现路由守卫（未登录跳转登录页）
4. 按页面逐个开发：登录注册 → 商家主页 → 档期日历 → 约单流程 → 帖子 Feed

### 要学什么

**项目初始化：**
```bash
npm create vite@latest cosplay-frontend -- --template vue-ts
cd cosplay-frontend
npm install
npm install vue-router pinia axios element-plus
```

**Axios 封装（带 token + 统一错误处理）：**
```typescript
// src/utils/request.ts
import axios from 'axios'
import { useUserStore } from '@/stores/user'
import router from '@/router'
import { ElMessage } from 'element-plus'

const request = axios.create({
    baseURL: '/api',
    timeout: 10000
})

request.interceptors.request.use(config => {
    const userStore = useUserStore()
    if (userStore.token) {
        config.headers['token'] = userStore.token
    }
    return config
})

request.interceptors.response.use(
    response => {
        const result = response.data
        if (result.code === 200) {
            return result.data
        }
        if (result.code === 401) {
            router.push('/login')
            return Promise.reject('未登录')
        }
        ElMessage.error(result.message)
        return Promise.reject(result.message)
    },
    error => {
        ElMessage.error('网络错误')
        return Promise.reject(error)
    }
)

export default request
```

**路由守卫：**
```typescript
// src/router/index.ts
router.beforeEach((to, from, next) => {
    const userStore = useUserStore()
    if (to.meta.requireAuth && !userStore.token) {
        next('/login')
    } else {
        next()
    }
})
```

**游标分页的前端实现（下拉加载）：**
```typescript
const posts = ref<Post[]>([])
const lastId = ref<number | null>(null)
const noMore = ref(false)

async function loadMore() {
    if (noMore.value) return
    const newPosts = await getPostList({ lastId: lastId.value, size: 10 })
    if (newPosts.length < 10) noMore.value = true
    if (newPosts.length > 0) {
        posts.value.push(...newPosts)
        lastId.value = newPosts[newPosts.length - 1].id
    }
}
```

### 学习资料

| 资源 | 说明 | 获取方式 |
|------|------|----------|
| Vue 3 官方文档（中文）| Composition API / 响应式 / 组件 / `<script setup>` | 直接访问 cn.vuejs.org |
| Element Plus 组件文档 | 所有组件的 props、事件和插槽说明 | 搜索：`element plus 文档` 进 element-plus.org |
| Pinia 官方文档（中文）| defineStore / storeToRefs / 插件 | 搜索：`pinia 官方文档` 进 pinia.vuejs.org |
| Vue Router 4 文档（中文）| 路由守卫 / 懒加载 / 动态路由 | 搜索：`vue router 4 文档` 进 router.vuejs.org |
| B站 Vue3 项目实战 | 跟着完整项目做一遍效果最好 | B站搜索：`尚硅谷 Vue3` 或 `黑马程序员 Vue3` |

**关键词速查**：`ref reactive computed watch Composition API defineStore Pinia axios拦截器 vue-router 路由守卫 游标分页`

### 面试题

**Q：Vue 3 Composition API 相比 Options API 的优势？**

A：逻辑复用更好（提取 `useXxx()` composable，无 mixin 命名冲突）；TypeScript 支持更完整；`<script setup>` 编译优化，代码更小；同一功能的代码聚合在一起，不像 Options API 分散在 data/methods/computed 各处。

**Q：Pinia 相比 Vuex 的优势？**

A：无 mutations，直接修改 state 更简单；完整 TypeScript 支持；多个 store 模块化自然；体积更小；支持 Vue DevTools。

---

## 第十三阶段：部署上线（第40-45天）

### 要做什么

1. 在服务器上创建项目目录，准备生产环境 `docker-compose.yml`
2. 安装 Nginx，配置反向代理
3. 申请 SSL 证书（免费），配置 HTTPS
4. 部署后端 jar 包和前端静态文件
5. 配置防火墙，只开放 80/443 端口

### 服务器目录结构

```
/home/cosplay/
├── docker-compose.yml    # 生产环境（不暴露 DB 端口）
├── nginx/
│   ├── nginx.conf
│   └── ssl/              # SSL 证书
├── backend/
│   └── app.jar           # Spring Boot 打包后的 jar
└── frontend/
    └── dist/             # Vue 3 打包后的静态文件
```

### 生产环境 docker-compose.yml

```yaml
version: '3.8'
services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: 强密码请修改
      MYSQL_DATABASE: cosplay_schedule
    volumes:
      - mysql_data:/var/lib/mysql
    # 生产环境不暴露端口到宿主机，只内部访问
    expose:
      - "3306"

  redis:
    image: redis:7.0
    command: redis-server --requirepass 强密码请修改
    expose:
      - "6379"

  rabbitmq:
    image: rabbitmq:3.12-management
    environment:
      RABBITMQ_DEFAULT_USER: admin
      RABBITMQ_DEFAULT_PASS: 强密码请修改
    expose:
      - "5672"
    ports:
      - "127.0.0.1:15672:15672"  # 管理界面只绑定本地

  app:
    image: openjdk:17-jre-slim
    working_dir: /app
    volumes:
      - ./backend:/app
    command: java -Xms256m -Xmx512m -jar app.jar --spring.profiles.active=prod
    ports:
      - "8081:8080"
    depends_on:
      - mysql
      - redis
      - rabbitmq

volumes:
  mysql_data:
```

### Nginx 配置

```nginx
# /home/cosplay/nginx/nginx.conf
server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$host$request_uri;  # 强制 HTTPS
}

server {
    listen 443 ssl;
    server_name your-domain.com;

    ssl_certificate     /etc/nginx/ssl/fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/privkey.pem;

    # 前端静态文件
    location / {
        root /home/cosplay/frontend/dist;
        index index.html;
        try_files $uri $uri/ /index.html;  # Vue Router history 模式
    }

    # 后端 API 反向代理
    location /api/ {
        proxy_pass http://localhost:8081;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### 免费 SSL 证书申请（Let's Encrypt）

```bash
# 安装 certbot
sudo apt install certbot python3-certbot-nginx   # Ubuntu
# 或
sudo yum install certbot python3-certbot-nginx   # CentOS

# 申请证书（域名必须已备案并解析到这台服务器）
sudo certbot --nginx -d your-domain.com

# 证书90天自动续期（certbot 安装后已自动配置）
sudo certbot renew --dry-run  # 测试续期是否正常
```

### 部署流程（每次更新代码时）

```bash
# 本地打包
mvn clean package -DskipTests
# 上传 jar 包到服务器
scp target/app.jar user@your-server:/home/cosplay/backend/

# 前端打包
npm run build
# 上传 dist 目录
scp -r dist/ user@your-server:/home/cosplay/frontend/

# 服务器上重启后端
docker compose restart app
```

### 学习资料

| 资源 | 说明 | 获取方式 |
|------|------|----------|
| Nginx 入门指南 | 反向代理 / location / try_files 配置 | 搜索：`nginx beginner guide` 进 nginx.org/en/docs |
| Certbot + Let's Encrypt | 免费 SSL 证书申请和自动续期命令 | 搜索：`certbot instructions nginx ubuntu` 进 certbot.eff.org |
| 掘金 Docker 生产部署最佳实践 | 安全配置 / 不暴露端口 / 容器日志 | 掘金搜索：`Docker Compose 生产环境部署` |

**关键词速查**：`Nginx proxy_pass location try_files ssl_certificate certbot Let's Encrypt 防火墙 ufw iptables`

---

## 补充：调研新功能实现要点

> 以下 4 个功能由调研驱动新增，数据库表已在第三阶段建好。本节说明后端接口和前端实现的关键点，开发顺序建议放在第十一阶段之后、前端阶段同步进行。

---

### 功能一：商家/客人搜索发现（第三阶段后即可加）

**场景**：调研显示 4/6 客人"不知道去哪找合适的商家"，这是首要发现功能。

**接口设计：**
```
GET /api/v1/merchant/search?city=北京&serviceType=1&keyword=美甲&page=1&size=10
```

**后端实现：**
```java
// MerchantMapper.xml（需要用 JSON_CONTAINS 查 service_types 字段）
@Select("SELECT * FROM merchant " +
        "WHERE (#{city} IS NULL OR city = #{city}) " +
        "AND (#{serviceType} IS NULL OR JSON_CONTAINS(service_types, CAST(#{serviceType} AS JSON))) " +
        "AND (#{keyword} IS NULL OR intro LIKE CONCAT('%', #{keyword}, '%')) " +
        "LIMIT #{offset}, #{size}")
List<Merchant> search(@Param("city") String city,
                      @Param("serviceType") Integer serviceType,
                      @Param("keyword") String keyword,
                      @Param("offset") int offset,
                      @Param("size") int size);
```

**前端：** 搜索栏 + 城市下拉 + 服务类型多选 → 触发接口 → 卡片列表展示。

---

### 功能二：评价系统（订单完成后解锁）

**场景**：客人完成订单后评分+文字+图片，商家可以回复，主页展示均分。

**接口清单：**
```
POST   /api/v1/review              # 提交评价（订单状态=3才能评，每单只能一次）
GET    /api/v1/review/merchant/{id}?page=1&size=10  # 商家评价列表
PUT    /api/v1/review/{id}/reply   # 商家回复（仅商家本人）
```

**后端核心逻辑：**
```java
public void createReview(CreateReviewDTO dto) {
    Long userId = StpUtil.getLoginIdAsLong();

    // 校验：订单必须是"已完成"状态且属于当前用户
    Order order = orderMapper.selectById(dto.getOrderId());
    if (order == null || !order.getUserId().equals(userId)) {
        throw new BusinessException("订单不存在");
    }
    if (order.getStatus() != OrderStatus.COMPLETED) {
        throw new BusinessException("订单未完成，不能评价");
    }

    // 防重（数据库 UNIQUE KEY uk_order_id 兜底）
    long count = reviewMapper.selectCount(
        new LambdaQueryWrapper<Review>().eq(Review::getOrderId, dto.getOrderId())
    );
    if (count > 0) {
        throw new BusinessException("已评价过该订单");
    }

    Review review = new Review();
    review.setOrderId(dto.getOrderId());
    review.setUserId(userId);
    review.setMerchantId(order.getMerchantId());
    review.setScore(dto.getScore());
    review.setContent(dto.getContent());
    reviewMapper.insert(review);

    // 更新商家均分（冗余字段，避免每次聚合查询）
    merchantMapper.updateAvgScore(order.getMerchantId());
}
```

```sql
-- 更新均分的 SQL（在 MerchantMapper.xml 中）
UPDATE merchant SET
    avg_score = (SELECT AVG(score) FROM review WHERE merchant_id = #{merchantId}),
    review_count = (SELECT COUNT(*) FROM review WHERE merchant_id = #{merchantId})
WHERE id = #{merchantId}
```

---

### 功能三：站内私信（WebSocket）

**场景**：调研显示5/6客人用微信沟通，希望能在平台直接联系商家。WebSocket 实现实时消息推送。

**添加依赖：**
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-websocket</artifactId>
</dependency>
```

**后端配置：**
```java
@Configuration
@EnableWebSocketMessageBroker
public class WebSocketConfig implements WebSocketMessageBrokerConfigurer {

    @Override
    public void configureMessageBroker(MessageBrokerRegistry registry) {
        registry.enableSimpleBroker("/topic", "/queue");
        registry.setApplicationDestinationPrefixes("/app");
        registry.setUserDestinationPrefix("/user");
    }

    @Override
    public void registerStompEndpoints(StompEndpointRegistry registry) {
        registry.addEndpoint("/ws")
                .setAllowedOriginPatterns("*")
                .withSockJS();
    }
}
```

**消息接口（发消息 + 历史记录）：**
```java
@RestController
public class MessageController {

    @Autowired
    private SimpMessagingTemplate messagingTemplate;
    @Autowired
    private MessageMapper messageMapper;

    // REST 接口：发送消息（同时持久化 + 推送）
    @PostMapping("/api/v1/message/send")
    public Result<?> send(@RequestBody SendMessageDTO dto) {
        Long fromUserId = StpUtil.getLoginIdAsLong();

        // 持久化到数据库
        Message msg = new Message();
        msg.setFromUserId(fromUserId);
        msg.setToUserId(dto.getToUserId());
        msg.setContent(dto.getContent());
        msg.setMsgType(dto.getMsgType());
        messageMapper.insert(msg);

        // 通过 WebSocket 推送给接收方（如果在线）
        messagingTemplate.convertAndSendToUser(
            dto.getToUserId().toString(),  // 目标用户
            "/queue/messages",
            msg
        );
        return Result.ok(msg);
    }

    // REST 接口：获取与某用户的历史消息（游标分页）
    @GetMapping("/api/v1/message/history")
    public Result<?> history(@RequestParam Long targetUserId,
                              @RequestParam(required = false) Long lastId,
                              @RequestParam(defaultValue = "20") int size) {
        Long userId = StpUtil.getLoginIdAsLong();
        List<Message> list = messageMapper.selectHistory(userId, targetUserId, lastId, size);
        return Result.ok(list);
    }
}
```

**前端接入（Vue 3 + @stomp/stompjs）：**
```bash
npm install sockjs-client @stomp/stompjs
```

```typescript
// src/composables/useWebSocket.ts
import { Client } from '@stomp/stompjs'
import SockJS from 'sockjs-client'
import { useUserStore } from '@/stores/user'

export function useWebSocket(onMessage: (msg: any) => void) {
    const userStore = useUserStore()
    const client = new Client({
        webSocketFactory: () => new SockJS('/ws'),
        connectHeaders: { token: userStore.token },
        onConnect: () => {
            client.subscribe(`/user/queue/messages`, frame => {
                onMessage(JSON.parse(frame.body))
            })
        }
    })
    client.activate()
    return client
}
```

> ⚠️ **注意**：WebSocket 的用户鉴权需要在 Sa-Token 拦截器里排除 `/ws/**` 路径，改为在连接握手时通过 `connectHeaders` 中的 token 手动验证。

**面试能展开：**
- Q：WebSocket 和 HTTP 长轮询的区别？A：长轮询每次请求都有 HTTP 头开销，WebSocket 一次握手后全双工通信，延迟更低、带宽占用更小。
- Q：如果用户不在线，消息怎么办？A：本项目消息持久化到 DB，用户上线后请求历史消息接口补拉。生产环境可结合 Redis 存"在线状态"判断是否需要推送还是仅入库。

---

### 功能四：妆前问卷（动态表单）

**场景**：商家可以创建问卷模板（含肤质、发型偏好等问题），客人下单时填写，答案随订单存储。

**后端接口：**
```
POST   /api/v1/questionnaire            # 商家创建问卷模板
GET    /api/v1/questionnaire/merchant/{id}  # 客人查看商家的问卷（约单前展示）
PUT    /api/v1/questionnaire/{id}        # 商家修改问卷
```

**问卷模板 JSON 格式（存入 questions 字段）：**
```json
[
  {"id": 1, "title": "肤质类型", "type": "radio", "options": ["干皮", "油皮", "混合肌", "敏感肌"], "required": true},
  {"id": 2, "title": "对妆容的偏好描述", "type": "text", "required": false},
  {"id": 3, "title": "参考图片链接", "type": "text", "required": false}
]
```

**前端动态渲染（Vue 3）：**
```vue
<template>
  <div v-for="q in questions" :key="q.id" class="question-item">
    <p>{{ q.title }} <span v-if="q.required" class="required">*</span></p>

    <!-- 单选题 -->
    <el-radio-group v-if="q.type === 'radio'" v-model="answers[q.id]">
      <el-radio v-for="opt in q.options" :key="opt" :label="opt">{{ opt }}</el-radio>
    </el-radio-group>

    <!-- 填空题 -->
    <el-input v-else-if="q.type === 'text'"
              v-model="answers[q.id]"
              :placeholder="`请填写${q.title}`" />
  </div>
</template>

<script setup lang="ts">
const answers = ref<Record<number, string>>({})

// 提交时将 answers 作为 questionnaire_answer 字段一起传入下单接口
function submitOrder() {
    createOrder({
        scheduleId: props.scheduleId,
        questionnaireAnswer: answers.value,
        remark: remark.value
    })
}
</script>
```

**后端接收（订单 DTO）：**
```java
public class CreateOrderDTO {
    @NotNull
    private Long scheduleId;

    private String remark;

    // 问卷答案，存入 order.questionnaire_answer（JSON 字段）
    private Map<Integer, String> questionnaireAnswer;
}
```

---

### 功能五：投诉系统（基础版）

**场景**：双方均可提交投诉，人工审核，记录存档。不做自动封禁（风险高），只做状态流转。

**接口：**
```
POST  /api/v1/complaint          # 提交投诉（需关联订单）
GET   /api/v1/complaint/my       # 查看我提交的投诉
```

**后端逻辑（简单 CRUD）：**
```java
@PostMapping("/api/v1/complaint")
public Result<?> submit(@RequestBody @Validated CreateComplaintDTO dto) {
    Long userId = StpUtil.getLoginIdAsLong();
    // 校验订单存在且当前用户是当事人之一
    Order order = orderMapper.selectById(dto.getOrderId());
    if (order == null) throw new BusinessException("订单不存在");
    if (!order.getUserId().equals(userId) && !order.getMerchantId().equals(userId)) {
        throw new BusinessException("无权投诉此订单");
    }
    Complaint complaint = new Complaint();
    complaint.setOrderId(dto.getOrderId());
    complaint.setComplainantId(userId);
    complaint.setRespondentId(
        order.getUserId().equals(userId) ? order.getMerchantId() : order.getUserId()
    );
    complaint.setReason(dto.getReason());
    complaintMapper.insert(complaint);
    return Result.ok("投诉已提交，等待处理");
}
```

> **面试提示**：可以说"投诉系统当前是人工审核基础版，设计时预留了 status 字段和 admin_reply 字段，后续可以加管理后台做在线处理，不需要改表结构"。这展示了对可扩展性的考量。

---

## 综合面试题

**Q：介绍一下你的项目亮点？**

A（话术模板）：
> 我做了一个面向二次元 cosplay 圈子的档期预约平台，解决了妆娘、摄影师管理预约效率低的问题。
>
> 技术上有几个亮点：第一，抢档期功能用 **Redis ZSet + Lua 脚本**保证排队的原子性，配合 **Sentinel** 做 QPS 限流，防止瞬间流量压垮服务。第二，通知模块用 **RabbitMQ** 解耦，通知失败不影响主流程，死信队列做兜底。第三，商家主页缓存用 Cache Aside 模式，针对穿透、击穿、雪崩分别做了处理。第四，订单超时取消用 **XXL-Job**，解决了多实例部署时 @Scheduled 重复执行的问题。

**Q：抢档期如果并发量很高，你的方案能撑住吗？**

A：做了三层保护：Sentinel 限流先挡掉超出阈值的请求；Redis Lua 脚本在内存中完成，单机支撑几万 QPS；MySQL 只在最终确认订单时写入，不在抢的瞬间写库。这个业务场景量级不大，更重要的是方案选型的思路，如果要支撑更高并发还可以加 Redis Cluster。

**Q：RabbitMQ 宕机了怎么办？**

A：RabbitMQ 本身支持集群部署保证高可用。生产者开了 Confirm 模式，发送失败记录本地消息表补偿重发。消费者手动 ACK，宕机时未 ACK 的消息重新投递。最坏情况完全不可用时，通知功能降级，但订单核心流程不受影响，这正是解耦的价值。

**Q：后续如果要做 Android App，需要改后端吗？**

A：基本不需要。设计时就考虑了这点：后端是纯 REST API；Token 通过 Header 传而不是 Cookie；接口加了 `/api/v1/` 版本号前缀；Feed 流用游标分页而不是页码分页，App 下拉加载不会出现重复数据。后续开发 Android App，只需要用 Retrofit2 调同一套接口，再写 Android 前端就行。

---

## 成本总览

```
服务器：已有，¥0 额外支出
域名：  约 ¥55/年
图片存储：七牛云，免费 10GB
SSL证书：Let's Encrypt，免费自动续期
━━━━━━━━━━━━━━━━━━━━━━━━━━
年均成本：¥55
两年总成本：¥110
```

## 整体时间线

```
准备工作（第1天）
  ├── 申请 JetBrains 学生授权
  ├── 安装本地开发环境
  ├── 服务器装好 Docker
  └── 买好域名

第1-2天     Docker 本地环境启动，四个服务全部运行正常
第3-12天    Spring Boot + MyBatis-Plus + 数据库设计
第10-12天   Sa-Token 认证
第13-20天   Redis（缓存 + 抢档期 Lua 脚本）
第18-20天   Redisson 分布式锁
第20-26天   RabbitMQ + 七牛云图片存储
第24-26天   XXL-Job 定时任务
第26-28天   Sentinel 限流
第28-42天   Vue 3 前端
第40-45天   部署上线 + 提交 ICP 备案
第45-50天   进阶增强（ThreadLocal / MapStruct / 延迟队列 / 两级缓存等）
第50-55天   可观测性（SkyWalking / Prometheus + Grafana）

备案通过后   正式绑定域名上线
```

---

## 进阶技术（第45天起，逐项叠加）

> 以下技术都是中大型公司项目中实际落地的方案。前13个阶段让你能跑起来，这些技术让你在面试中脱颖而出。建议按顺序做，每项都能在本项目中找到真实落地场景。

---

## 第十四阶段：ThreadLocal 用户上下文（第3天起就应该用）

### 要做什么

封装 `UserContext` 工具类，在 Sa-Token 拦截器里将当前登录用户信息写入 `ThreadLocal`，业务层直接调 `UserContext.getUserId()` 而不是每次写 `StpUtil.getLoginIdAsLong()`。

这是几乎所有公司项目都有的基础设施，非常简单但面试必问。

### 要学什么

```java
// 1. 用户上下文持有者
public class UserContext {
    private static final ThreadLocal<UserInfo> USER_THREAD_LOCAL = new ThreadLocal<>();

    public static void set(UserInfo user) { USER_THREAD_LOCAL.set(user); }
    public static UserInfo get() { return USER_THREAD_LOCAL.get(); }
    public static Long getUserId() { return get().getId(); }
    public static String getRole() { return get().getRole(); }

    // ⚠️ 必须提供 remove()，防止线程池复用时数据串位（内存泄漏根因）
    public static void remove() { USER_THREAD_LOCAL.remove(); }
}

// 2. 在 Sa-Token 拦截器之后再加一个拦截器，写入上下文
@Component
public class UserContextInterceptor implements HandlerInterceptor {

    @Override
    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) {
        // Sa-Token 已验证 token，这里直接取用户ID查用户信息
        if (StpUtil.isLogin()) {
            Long userId = StpUtil.getLoginIdAsLong();
            // 可从 Redis 缓存取，避免每次查库
            UserInfo userInfo = userCacheService.getUserInfo(userId);
            UserContext.set(userInfo);
        }
        return true;
    }

    @Override
    public void afterCompletion(HttpServletRequest request, HttpServletResponse response,
                                Object handler, Exception ex) {
        // ⚠️ 必须清理，否则线程池复用该线程时拿到上一个请求的用户
        UserContext.remove();
    }
}

// 3. 业务层使用（告别到处写 StpUtil.getLoginIdAsLong()）
public void createOrder(CreateOrderDTO dto) {
    Long userId = UserContext.getUserId();  // 简洁
    String role = UserContext.getRole();    // 直接取
    // ...
}
```

**父子线程传值**：`ThreadLocal` 对子线程不可见。用 `TransmittableThreadLocal`（阿里 TTL 库）解决线程池场景下的父子线程值传递。

```xml
<dependency>
    <groupId>com.alibaba</groupId>
    <artifactId>transmittable-thread-local</artifactId>
    <version>2.14.4</version>
</dependency>
```

```java
// 替换 ThreadLocal → TransmittableThreadLocal，子线程自动继承父线程的值
private static final TransmittableThreadLocal<UserInfo> USER_THREAD_LOCAL = new TransmittableThreadLocal<>();
```

### 验收标准

- 所有业务层代码中不再出现 `StpUtil.getLoginIdAsLong()`，统一走 `UserContext.getUserId()`
- 用 Jmeter 并发测试，不同用户并发请求不会串位（各自拿到自己的用户信息）

### 面试题

**Q：ThreadLocal 为什么会内存泄漏？**

A：`ThreadLocal` 的 key 是弱引用，GC 时 key 会被回收变成 null，但 value 是强引用不会被回收。在线程池场景下，线程复用不会销毁，value 就永久挂在 `ThreadLocalMap` 里无法被 GC，越积越多。解决方案：每次请求结束后必须调 `remove()`，在拦截器的 `afterCompletion` 中清理。

**Q：为什么用 `TransmittableThreadLocal` 而不是 `InheritableThreadLocal`？**

A：`InheritableThreadLocal` 只在线程创建时拷贝父线程的值（创建一次）。线程池中的线程是复用的，不会再次创建，所以 `InheritableThreadLocal` 在线程池中不生效。`TransmittableThreadLocal` 在任务提交到线程池时主动将父线程的值拷贝给子线程，每次提交都拷贝，解决了这个问题。

---

## 第十五阶段：MapStruct 对象映射（第3天起替换手写转换）

### 要做什么

项目中 Entity（数据库实体）、DTO（接收前端参数）、VO（返回给前端）三类对象之间需要互转。用 MapStruct 代替手写 `BeanUtils.copyProperties()`，编译期生成转换代码，类型安全且零反射开销。

### 要学什么

```xml
<dependency>
    <groupId>org.mapstruct</groupId>
    <artifactId>mapstruct</artifactId>
    <version>1.5.5.Final</version>
</dependency>
<dependency>
    <groupId>org.mapstruct</groupId>
    <artifactId>mapstruct-processor</artifactId>
    <version>1.5.5.Final</version>
    <scope>provided</scope>
</dependency>
```

```java
// 1. 简单转换：字段名相同，直接映射
@Mapper(componentModel = "spring")  // 生成 Spring Bean，可 @Autowired
public interface MerchantConverter {
    MerchantVO toVO(Merchant merchant);
    Merchant toEntity(CreateMerchantDTO dto);
    List<MerchantVO> toVOList(List<Merchant> list);
}

// 2. 字段名不同 / 需要表达式
@Mapper(componentModel = "spring")
public interface UserConverter {

    @Mapping(source = "phone", target = "maskedPhone",
             expression = "java(maskPhone(user.getPhone()))")
    @Mapping(source = "birthday", target = "age",
             expression = "java(calculateAge(user.getBirthday()))")
    UserVO toVO(User user);

    // 辅助方法：直接写在接口里（default方法）
    default String maskPhone(String phone) {
        if (phone == null || phone.length() != 11) return phone;
        return phone.substring(0, 3) + "****" + phone.substring(7);
    }

    default Integer calculateAge(LocalDate birthday) {
        if (birthday == null) return null;
        return Period.between(birthday, LocalDate.now()).getYears();
    }
}

// 3. 多源合并（将多个对象合并成一个VO）
@Mapper(componentModel = "spring")
public interface OrderConverter {

    @Mapping(source = "order.id", target = "orderId")
    @Mapping(source = "order.status", target = "status")
    @Mapping(source = "merchant.nickname", target = "merchantName")
    @Mapping(source = "user.nickname", target = "userName")
    OrderDetailVO toDetailVO(Order order, Merchant merchant, User user);
}

// 4. 使用（注入即用，编译后生成实现类）
@Service
public class MerchantService {
    @Autowired
    private MerchantConverter merchantConverter;

    public MerchantVO getMerchant(Long id) {
        Merchant merchant = merchantMapper.selectById(id);
        return merchantConverter.toVO(merchant);   // 替代 BeanUtils.copyProperties
    }
}
```

> ⚠️ **Lombok + MapStruct 共存注意**：`maven-compiler-plugin` 中要确保 `mapstruct-processor` 在 `lombok` 之后处理，否则 `@Data` 生成的 getter/setter MapStruct 找不到。

```xml
<!-- pom.xml 插件配置 -->
<plugin>
    <groupId>org.apache.maven.plugins</groupId>
    <artifactId>maven-compiler-plugin</artifactId>
    <configuration>
        <annotationProcessorPaths>
            <path><groupId>org.projectlombok</groupId><artifactId>lombok</artifactId><version>1.18.30</version></path>
            <path><groupId>org.mapstruct</groupId><artifactId>mapstruct-processor</artifactId><version>1.5.5.Final</version></path>
        </annotationProcessorPaths>
    </configuration>
</plugin>
```

### 验收标准

- 项目中删除所有 `BeanUtils.copyProperties()` 调用
- 手机号在 UserVO 中显示为 `138****8888` 格式
- 年龄由 birthday 动态计算，不存库

### 面试题

**Q：MapStruct 和 BeanUtils 的区别？**

A：`BeanUtils.copyProperties()` 通过反射在运行期匹配字段，有性能损耗，字段名写错只有运行时才报错，且没有类型转换能力。MapStruct 是编译期代码生成，生成的是纯 Java 赋值代码（`target.setName(source.getName())`），运行期零反射开销，字段名不匹配在编译期就报错，还支持表达式、多源合并等复杂映射。

**Q：项目中哪里用到了 MapStruct？**

A：三处典型：① `User → UserVO` 时顺便做手机号脱敏和年龄计算；② `Order + Merchant + User → OrderDetailVO` 多源合并，避免在 Service 层写大量赋值代码；③ `List<Merchant> → List<MerchantVO>` 批量转换，一行代码替代 `stream().map().collect()`。

---

## 第十六阶段：延迟队列——RabbitMQ TTL + 死信队列（第20天，替代 XXL-Job 超时取消）

### 要做什么

订单超时取消用 XXL-Job 每分钟轮询有最多 1 分钟延迟，且随数据量增长扫表压力变大。**延迟队列**方案：下单时向 MQ 发一条 30 分钟后才可消费的消息，到期后消费者直接取消，不需要扫表。精度更高、压库更少、面试更加分。

### 要学什么

**方案一：TTL + 死信交换机（无需额外插件，推荐）**

```java
// 1. 配置：普通队列设 TTL，到期转入死信队列
@Configuration
public class DelayQueueConfig {

    // 死信交换机（接收超时/拒绝的消息）
    @Bean
    public DirectExchange deadExchange() {
        return new DirectExchange("order.dead.exchange");
    }

    // 死信队列（真正消费超时订单的队列）
    @Bean
    public Queue deadQueue() {
        return QueueBuilder.durable("order.dead.queue").build();
    }

    @Bean
    public Binding deadBinding() {
        return BindingBuilder.bind(deadQueue()).to(deadExchange()).with("order.timeout");
    }

    // 延迟队列：消息 TTL 30分钟，到期后路由到死信交换机
    @Bean
    public Queue orderDelayQueue() {
        return QueueBuilder.durable("order.delay.queue")
            .withArgument("x-dead-letter-exchange", "order.dead.exchange")
            .withArgument("x-dead-letter-routing-key", "order.timeout")
            .withArgument("x-message-ttl", 30 * 60 * 1000)  // 30分钟 ms
            .build();
    }

    @Bean
    public DirectExchange orderDelayExchange() {
        return new DirectExchange("order.delay.exchange");
    }

    @Bean
    public Binding orderDelayBinding() {
        return BindingBuilder.bind(orderDelayQueue()).to(orderDelayExchange()).with("order.delay");
    }
}

// 2. 下单时发送延迟消息（替代 XXL-Job 超时任务）
@Service
public class OrderService {

    public void createOrder(CreateOrderDTO dto) {
        // ... 正常创建订单逻辑
        Order order = buildOrder(dto);
        orderMapper.insert(order);

        // 发延迟消息：30分钟后如果订单还是"待确认"就取消
        rabbitTemplate.convertAndSend(
            "order.delay.exchange",
            "order.delay",
            order.getOrderNo()  // 只传订单号，消费时查库获取最新状态
        );
    }
}

// 3. 消费者：收到消息时订单已经过了30分钟
@RabbitListener(queues = "order.dead.queue")
public void handleTimeout(String orderNo, Channel channel,
                           @Header(AmqpHeaders.DELIVERY_TAG) long tag) throws IOException {
    try {
        Order order = orderMapper.selectByOrderNo(orderNo);
        // 只有"待确认"状态才取消，已确认/已完成的不处理
        if (order != null && order.getStatus() == OrderStatus.PENDING) {
            orderService.cancel(order.getId(), "超时未确认自动取消");
        }
        channel.basicAck(tag, false);
    } catch (Exception e) {
        channel.basicNack(tag, false, false);  // 进死信队列兜底
    }
}
```

**方案二：RabbitMQ 延迟插件（`rabbitmq_delayed_message_exchange`）**

插件允许每条消息设置不同的延迟时间（TTL方案全队列统一TTL），适合不同订单类型有不同超时时间的场景。

```bash
# docker-compose.yml 改用带插件的镜像
image: rabbitmq:3.12-management-plugins  # 或手动安装插件
```

```java
// 插件方式：发消息时在 header 里指定延迟毫秒数
rabbitTemplate.convertAndSend("delay.exchange", "order.timeout", orderNo, message -> {
    message.getMessageProperties().setDelay(30 * 60 * 1000);  // 每条消息自定义延迟
    return message;
});
```

### 与 XXL-Job 的对比

| 维度 | XXL-Job 轮询 | 延迟队列 |
|------|-------------|---------|
| 精度 | 最多1分钟误差 | 秒级 |
| 压库 | 每分钟扫全表 | 不扫表，到期推送 |
| 可靠性 | 任务失败需重试机制 | MQ持久化+死信兜底 |
| 复杂度 | 简单，可在控制台查看 | 稍复杂，需配死信 |

> 本项目两种方案都保留：`XXL-Job` 作为兜底（每小时扫一次漏网的），延迟队列作为主路径。面试时这样回答展示了防御性设计思维。

### 面试题

**Q：为什么不用 Java 自带的 `DelayQueue`？**

A：`DelayQueue` 是单机内存方案，服务重启消息丢失，多实例部署时任务不共享，无法在分布式场景使用。RabbitMQ 的延迟队列消息持久化到磁盘，服务重启不丢，多实例只有一个消费者处理（MQ 默认轮询分发）。

**Q：TTL + 死信队列方案有什么缺点？**

A：同一队列的所有消息 TTL 相同，如果需要不同延迟时间（比如 VIP 用户30分钟、普通用户15分钟），就要建多个队列。另外，如果队头消息未过期，后面的消息即使到期也不会被路由（因为 RabbitMQ 只检查队头）。延迟插件方案解决了这个问题，每条消息独立计时。

**Q：消息已经发出去了，用户手动取消了订单，延迟消息怎么处理？**

A：不需要撤回消息（MQ 没有高效撤回机制）。消费者收到消息时先查库获取订单最新状态，如果已取消/已完成就直接 ACK 不处理。"下单时发消息，消费时查最新状态"是延迟队列的标准处理模式，解耦了消息与业务状态。

---

## 第十七阶段：两级缓存——Caffeine + Redis（第20天，高并发读优化）

### 要做什么

商家主页是读多写少的高频接口。纯 Redis 缓存每次还需网络 IO（1-5ms），高并发时 Redis 也承压。加入 **Caffeine 本地缓存**作为 L1，Redis 作为 L2：L1 命中直接返回（微秒级），L1 未命中再查 L2，L2 未命中才查数据库。

### 要学什么

```xml
<dependency>
    <groupId>com.github.ben-manes.caffeine</groupId>
    <artifactId>caffeine</artifactId>
    <!-- Spring Boot 3 已内置，无需指定版本 -->
</dependency>
```

```java
// 1. 配置 Caffeine 本地缓存
@Configuration
public class CaffeineConfig {

    @Bean
    public Cache<Long, MerchantVO> merchantLocalCache() {
        return Caffeine.newBuilder()
            .maximumSize(1000)           // 最多缓存1000个商家
            .expireAfterWrite(5, TimeUnit.MINUTES)  // 写入后5分钟过期
            .recordStats()               // 开启统计（命中率监控）
            .build();
    }
}

// 2. 两级缓存 Service
@Service
public class MerchantCacheService {

    @Autowired private Cache<Long, MerchantVO> merchantLocalCache;  // L1
    @Autowired private StringRedisTemplate redisTemplate;           // L2
    @Autowired private MerchantMapper merchantMapper;

    private static final String REDIS_KEY_PREFIX = "merchant:info:";
    private static final long REDIS_TTL_MINUTES = 30;

    public MerchantVO getMerchantVO(Long merchantId) {
        // L1：本地缓存（微秒级）
        MerchantVO cached = merchantLocalCache.getIfPresent(merchantId);
        if (cached != null) return cached;

        // L2：Redis（毫秒级）
        String json = redisTemplate.opsForValue().get(REDIS_KEY_PREFIX + merchantId);
        if (json != null) {
            MerchantVO vo = JSONUtil.toBean(json, MerchantVO.class);
            merchantLocalCache.put(merchantId, vo);  // 回填 L1
            return vo;
        }

        // DB：数据库（毫秒~秒级）
        Merchant merchant = merchantMapper.selectById(merchantId);
        if (merchant == null) throw new BusinessException("商家不存在");

        MerchantVO vo = merchantConverter.toVO(merchant);
        // 同时写入 L2 和 L1
        redisTemplate.opsForValue().set(
            REDIS_KEY_PREFIX + merchantId,
            JSONUtil.toJsonStr(vo),
            REDIS_TTL_MINUTES + new Random().nextInt(10),  // 随机TTL防雪崩
            TimeUnit.MINUTES
        );
        merchantLocalCache.put(merchantId, vo);
        return vo;
    }

    // 商家信息修改时，清除两级缓存
    public void evict(Long merchantId) {
        merchantLocalCache.invalidate(merchantId);               // 清 L1
        redisTemplate.delete(REDIS_KEY_PREFIX + merchantId);     // 清 L2
    }
}
```

**多实例部署时 L1 一致性问题**：实例 A 修改商家信息后，只能清自己的本地缓存，实例 B 的本地缓存还是旧数据。解决方案：清缓存时同时发一条 RabbitMQ 广播消息，所有实例收到后各自清本地缓存。

```java
// 清缓存时广播
public void evict(Long merchantId) {
    merchantLocalCache.invalidate(merchantId);
    redisTemplate.delete(REDIS_KEY_PREFIX + merchantId);
    // 广播给其他实例（Fanout交换机，每个实例都有自己的队列）
    rabbitTemplate.convertAndSend("cache.fanout.exchange", "", merchantId.toString());
}

// 每个实例监听，收到后清自己的本地缓存
@RabbitListener(queues = "#{cacheEvictQueue.name}")  // 每个实例独立队列
public void onCacheEvict(String merchantIdStr) {
    merchantLocalCache.invalidate(Long.parseLong(merchantIdStr));
}
```

### 验收标准

- 第一次请求查数据库，第二次命中 Redis，第三次命中本地缓存（通过日志/统计观察）
- 修改商家信息后，所有实例的缓存都更新（多开两个实例验证）
- `merchantLocalCache.stats()` 可以看到命中率

### 面试题

**Q：两级缓存的一致性如何保证？**

A：写操作时先更新数据库，再删 Redis，再通过 MQ 广播清所有实例的本地缓存（最终一致性）。本地缓存的 TTL 设置短一些（5分钟），就算广播失败，最多5分钟后本地缓存自动过期，从 Redis 拉最新数据。这是牺牲强一致性换取高性能的典型取舍，对商家信息这种非强实时场景是合理的。

**Q：为什么本地缓存 TTL 比 Redis 短？**

A：本地缓存没有集中管控，一旦数据不一致只能靠 TTL 自然过期修复，所以要设得短（3-5分钟）。Redis 有集中清除机制，一致性更容易保证，TTL 可以设长一些（30分钟）减少数据库压力。

---

## 第十八阶段：布隆过滤器——Redisson BloomFilter（第14天升级缓存穿透方案）

### 要做什么

第六阶段用空字符串缓存防穿透，缺点是：① 每个不存在的 ID 都占一个 Redis key；② 空字符串需要设 TTL，数据真正创建后短期内还会穿透。布隆过滤器用极小内存（1亿数据约 128MB）判断 ID 是否**可能存在**，在查 Redis 之前拦截掉大量无效请求。

### 要学什么

```java
// 1. 初始化布隆过滤器（项目启动时，从数据库加载所有已有ID）
@Component
public class BloomFilterManager {

    @Autowired private RedissonClient redissonClient;
    @Autowired private MerchantMapper merchantMapper;

    private RBloomFilter<Long> merchantBloomFilter;

    @PostConstruct  // Spring 启动时自动初始化
    public void init() {
        merchantBloomFilter = redissonClient.getBloomFilter("bloom:merchant");
        // 预期插入100万数据，误判率0.01（1%）
        merchantBloomFilter.tryInit(1_000_000L, 0.01);

        // 从数据库加载所有存在的商家ID（冷启动一次性操作）
        List<Long> ids = merchantMapper.selectAllIds();
        ids.forEach(merchantBloomFilter::add);
        log.info("布隆过滤器初始化完成，加载商家ID: {} 个", ids.size());
    }

    public boolean mightExist(Long merchantId) {
        return merchantBloomFilter.contains(merchantId);
    }

    // 新增商家时，同步加入布隆过滤器
    public void add(Long merchantId) {
        merchantBloomFilter.add(merchantId);
    }
}

// 2. 在缓存查询前加一层布隆过滤器拦截
public MerchantVO getMerchantVO(Long merchantId) {
    // ⚡ 布隆过滤器：不存在的ID直接拒绝（没有误判率的那一侧）
    if (!bloomFilterManager.mightExist(merchantId)) {
        throw new BusinessException("商家不存在");
    }

    // 后续走正常缓存逻辑（两级缓存 → 数据库）
    // 省略...
}
```

**布隆过滤器 vs 空字符串缓存对比：**

| | 空字符串 | 布隆过滤器 |
|---|---|---|
| 内存占用 | 每个无效ID占一个Key | 固定内存，1亿数据约128MB |
| 误判情况 | 无误判（精确） | 有一定误判率（可配置） |
| 适用场景 | 无效ID少，有规律 | 无效ID海量，随机攻击 |
| 能否删除 | 可删 | 不能删（只能全量重建）|

### 面试题

**Q：布隆过滤器的原理是什么？**

A：布隆过滤器是一个大位数组 + 多个 Hash 函数。添加元素时，用多个 Hash 函数计算多个下标，将对应位置设为 1。查询时同样计算多个下标，如果所有位置都是 1，则"可能存在"；只要有一个位置是 0，则"一定不存在"。误判（false positive）是因为多个不同元素的 Hash 可能重叠导致"误判为存在"，但不存在的元素绝不会被判断为存在（false negative 为零）。

**Q：布隆过滤器为什么不能删除元素？**

A：一个位置可能被多个元素共用（Hash 碰撞），删除一个元素会把位置置 0，但这个位置可能还对应其他元素，导致其他元素误判为不存在。如果业务需要支持删除，用 **Counting Bloom Filter**（每个位置存计数而非0/1，删除时减1），但内存占用更大。

---

## 第十九阶段：CompletableFuture 异步编排 + 自定义线程池（第20天）

### 要做什么

商家详情页需要同时展示：商家基本信息 + 评价列表 + 帖子列表 + 档期日历。串行查询耗时 = 四次 IO 累加。并行查询耗时 = 最慢的那次 IO。用 `CompletableFuture` 将四次查询并行发出，接口响应时间降低 50-70%。

### 要学什么

```java
// 1. 先配置自定义线程池（不要用默认的 ForkJoinPool，IO密集要用有界队列）
@Configuration
public class ThreadPoolConfig {

    @Bean("ioThreadPool")
    public ThreadPoolExecutor ioThreadPool() {
        return new ThreadPoolExecutor(
            10,                                        // 核心线程数
            20,                                        // 最大线程数
            60, TimeUnit.SECONDS,                      // 空闲线程存活时间
            new ArrayBlockingQueue<>(500),             // 有界队列，防止OOM
            new ThreadFactoryBuilder().setNameFormat("io-pool-%d").build(),
            new ThreadPoolExecutor.CallerRunsPolicy()  // 队列满了，调用者线程执行（不丢任务）
        );
    }
}

// 2. 商家详情页并行聚合
@Service
public class MerchantDetailService {

    @Autowired @Qualifier("ioThreadPool")
    private ThreadPoolExecutor ioThreadPool;

    public MerchantDetailVO getMerchantDetail(Long merchantId) {
        // 四个查询并行发出
        CompletableFuture<MerchantVO> merchantFuture = CompletableFuture.supplyAsync(
            () -> merchantCacheService.getMerchantVO(merchantId), ioThreadPool
        );
        CompletableFuture<List<ReviewVO>> reviewFuture = CompletableFuture.supplyAsync(
            () -> reviewService.getLatestReviews(merchantId, 5), ioThreadPool
        );
        CompletableFuture<List<PostVO>> postFuture = CompletableFuture.supplyAsync(
            () -> postService.getLatestPosts(merchantId, 6), ioThreadPool
        );
        CompletableFuture<List<ScheduleVO>> scheduleFuture = CompletableFuture.supplyAsync(
            () -> scheduleService.getMonthSchedules(merchantId, YearMonth.now()), ioThreadPool
        );

        // 等待所有任务完成
        CompletableFuture.allOf(merchantFuture, reviewFuture, postFuture, scheduleFuture).join();

        // 组装结果
        MerchantDetailVO detail = new MerchantDetailVO();
        detail.setMerchant(merchantFuture.join());
        detail.setReviews(reviewFuture.join());
        detail.setPosts(postFuture.join());
        detail.setSchedules(scheduleFuture.join());
        return detail;
    }
}

// 3. 异常处理（任一任务失败，整体返回错误）
CompletableFuture<MerchantVO> safeFuture = merchantFuture
    .exceptionally(ex -> {
        log.error("查询商家信息失败", ex);
        return null;  // 返回降级值而不是抛出
    });
```

**Java 21 虚拟线程加成**：Java 21 + Spring Boot 3.2 开启虚拟线程后，IO 阻塞时虚拟线程自动挂起释放平台线程，线程池可以设更小的核心线程数处理更高并发。详见第二十四阶段。

### 面试题

**Q：`thenApply` 和 `thenCompose` 的区别？**

A：`thenApply` 接收上一步结果，返回一个普通值（同步转换）；`thenCompose` 接收上一步结果，返回一个新的 `CompletableFuture`（适合链式异步调用，避免嵌套）。类比 `Stream.map()` vs `Stream.flatMap()`。

**Q：`join()` 和 `get()` 的区别？**

A：`join()` 抛出未检查异常（`CompletionException`，是 `RuntimeException`），不需要 try-catch；`get()` 抛出检查异常（`ExecutionException` / `InterruptedException`），必须处理。实际项目中用 `join()` 更简洁。

**Q：为什么不用默认的 `ForkJoinPool`（`CompletableFuture.supplyAsync()` 无参版本）？**

A：默认 `ForkJoinPool` 是 CPU 核数 - 1 个线程，适合 CPU 密集计算。查数据库/Redis 是 IO 密集，线程大部分时间在等待，应该用更多线程的自定义线程池，让 CPU 趁等待 IO 的时候处理其他任务。此外 `ForkJoinPool` 是全局共享的，不同业务相互影响，自定义线程池可以做资源隔离。

---

## 第二十阶段：接口幂等性——Token 机制（第10天，下单防重复提交）

### 要做什么

用户网络差时多次点击"提交预约"，后端收到多个重复请求。第七阶段用 Redisson 锁解决了并发创建订单问题，但锁解决的是**并发**，幂等 Token 解决的是**重复提交**（用户主动多次点击）。两种方案配合使用，是公司中最常见的防重复提交标准做法。

### 要学什么

```java
// 1. 获取幂等 Token 接口（前端在打开表单时调用）
@GetMapping("/api/v1/idempotent/token")
public Result<String> getToken() {
    String token = IdUtil.fastSimpleUUID();
    // 存 Redis，5分钟有效期（用户5分钟内没提交就失效）
    stringRedisTemplate.opsForValue().set(
        "idempotent:token:" + token, "1", 5, TimeUnit.MINUTES
    );
    return Result.ok(token);
}

// 2. 自定义注解，标记需要幂等的接口
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface Idempotent {
    String message() default "请勿重复提交";
}

// 3. AOP 切面处理
@Aspect
@Component
public class IdempotentAspect {

    @Autowired private StringRedisTemplate redisTemplate;

    @Around("@annotation(idempotent)")
    public Object around(ProceedingJoinPoint pjp, Idempotent idempotent) throws Throwable {
        HttpServletRequest request = ((ServletRequestAttributes)
            RequestContextHolder.getRequestAttributes()).getRequest();
        String token = request.getHeader("Idempotent-Token");

        if (StrUtil.isBlank(token)) {
            throw new BusinessException("缺少幂等Token");
        }

        String key = "idempotent:token:" + token;
        // 原子性"查+删"：SET NX 不行（是先查再删两步），用 Lua 脚本
        String script = "if redis.call('get', KEYS[1]) == '1' then " +
                        "return redis.call('del', KEYS[1]) else return 0 end";
        Long result = redisTemplate.execute(
            new DefaultRedisScript<>(script, Long.class),
            Collections.singletonList(key)
        );

        if (result == null || result == 0) {
            throw new BusinessException(idempotent.message());  // Token已用过或已过期
        }

        return pjp.proceed();  // Token 验证通过，执行业务
    }
}

// 4. 在需要幂等的接口上加注解
@Idempotent(message = "请勿重复提交预约")
@PostMapping("/api/v1/order")
public Result<?> createOrder(@RequestBody @Validated CreateOrderDTO dto) {
    return orderService.create(dto);
}

// 5. 前端：打开约单页面时获取Token，提交时带上 Header
// 获取Token
const token = await getIdempotentToken()

// 提交时带 Header
axios.post('/api/v1/order', dto, {
    headers: { 'Idempotent-Token': token }
})
```

### 面试题

**Q：幂等 Token 为什么要用 Lua 脚本做"查+删"，而不是 Redis 的 `del` 命令？**

A：如果先 `get` 判断存在，再 `del` 删除，是两步操作，在高并发下两步之间可能插入其他线程也执行了 `get`，两个请求都拿到"存在"，都通过了幂等校验，导致重复处理。Lua 脚本在 Redis 中原子执行，查到就删、删完才返回，保证只有第一个请求成功，后续的全部拒绝。

**Q：幂等 Token 方案和 Redisson 分布式锁的区别，什么时候用哪个？**

A：两者都能防重复，但角度不同。**分布式锁**防的是**并发**：同一时刻多个线程同时执行，锁保证只有一个线程进入；锁释放后，其他线程还可以继续执行（比如重试）。**幂等 Token**防的是**重复**：同一个业务请求只能成功一次，Token 消费后无论再来多少次相同请求都拒绝，不存在"释放后重试"的概念。下单场景两者配合：Token 防用户多次点击，锁防网络重传导致的极短时间内并发到达。

---

## 第二十一阶段：AOP 操作日志（第10天，贯穿整个项目）

### 要做什么

商家确认订单、客人取消订单、管理员处理投诉……这些关键操作需要留存日志，方便事后审计和排查问题。用 AOP + 自定义注解实现，对业务代码零侵入。这是几乎每个正式项目都有的基础设施。

### 要学什么

```java
// 1. 自定义注解
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface OperationLog {
    String module() default "";   // 模块名，如"订单管理"
    String type() default "";     // 操作类型，如"确认预约"
}

// 2. 操作日志实体（需对应建一张 operation_log 表）
@Data
@TableName("operation_log")
public class OperationLog {
    private Long id;
    private Long userId;
    private String username;
    private String module;
    private String type;
    private String method;         // 全限定方法名
    private String params;         // 入参 JSON
    private String result;         // 出参 JSON（正常）
    private String errorMsg;       // 异常信息（失败）
    private Long costMs;           // 耗时（毫秒）
    private Integer status;        // 0成功 1失败
    private String ip;
    private LocalDateTime createTime;
}

// 3. AOP 切面
@Aspect
@Component
@Slf4j
public class OperationLogAspect {

    @Autowired private OperationLogMapper operationLogMapper;
    @Autowired @Qualifier("ioThreadPool") private ThreadPoolExecutor threadPool;

    @Around("@annotation(logAnnotation)")
    public Object around(ProceedingJoinPoint pjp,
                         com.cosplay.annotation.OperationLog logAnnotation) throws Throwable {
        long start = System.currentTimeMillis();
        OperationLog log = new OperationLog();
        log.setModule(logAnnotation.module());
        log.setType(logAnnotation.type());
        log.setMethod(pjp.getSignature().toShortString());
        log.setParams(JSONUtil.toJsonStr(pjp.getArgs()));
        log.setUserId(UserContext.getUserId());
        log.setIp(getClientIp());

        try {
            Object result = pjp.proceed();
            log.setResult(JSONUtil.toJsonStr(result));
            log.setStatus(0);
            return result;
        } catch (Throwable e) {
            log.setErrorMsg(e.getMessage());
            log.setStatus(1);
            throw e;
        } finally {
            log.setCostMs(System.currentTimeMillis() - start);
            // 异步写入，不阻塞主业务
            threadPool.execute(() -> operationLogMapper.insert(log));
        }
    }
}

// 4. 在关键接口上加注解
@OperationLog(module = "订单管理", type = "确认预约")
@PutMapping("/api/v1/order/{id}/confirm")
public Result<?> confirmOrder(@PathVariable Long id) { ... }

@OperationLog(module = "订单管理", type = "取消预约")
@PutMapping("/api/v1/order/{id}/cancel")
public Result<?> cancelOrder(@PathVariable Long id) { ... }
```

```sql
-- 操作日志表（分月分表或直接存，小项目单表够）
CREATE TABLE operation_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT,
    username VARCHAR(50),
    module VARCHAR(50),
    type VARCHAR(50),
    method VARCHAR(200),
    params TEXT,
    result TEXT,
    error_msg TEXT,
    cost_ms BIGINT,
    status TINYINT COMMENT '0:成功 1:失败',
    ip VARCHAR(50),
    create_time DATETIME,
    KEY idx_user_time (user_id, create_time),
    KEY idx_module (module, create_time)
);
```

### 面试题

**Q：AOP 的 `@Around`、`@Before`、`@After` 如何选择？**

A：`@Before` 在方法执行前，拿不到返回值；`@AfterReturning` 在正常返回后，拿得到返回值但拿不到异常；`@AfterThrowing` 在抛出异常时，`@After` 无论正常还是异常都执行。`@Around` 最强大，包裹整个方法，可以拿到入参、出参、异常、耗时，还可以修改入参或返回值。操作日志需要同时记录出参和异常，所以必须用 `@Around`。

**Q：为什么操作日志要异步写入？**

A：日志写入是非核心逻辑，不应该影响主业务的响应时间。如果同步写入，数据库慢时会直接拖慢接口。用线程池异步写入，主业务返回后日志在后台处理，两者解耦。代价是极少情况下服务宕机会丢失少量日志，对于操作审计日志来说是可以接受的。

---

## 第二十二阶段：读写分离——MySQL 主从 + Dynamic DataSource（第45天）

### 要做什么

随着数据量增长，读操作（查商家列表、查订单、查评价）远多于写操作，主库压力集中在读上。**读写分离**：所有写操作（insert/update/delete）走主库，所有读操作（select）走从库。一主一从可以把主库 QPS 降低 70% 以上。

### 要学什么

```xml
<dependency>
    <groupId>com.baomidou</groupId>
    <artifactId>dynamic-datasource-spring-boot3-starter</artifactId>
    <version>4.3.0</version>
</dependency>
```

```yaml
# application.yml
spring:
  datasource:
    dynamic:
      primary: master         # 默认走主库
      strict: false           # 找不到指定数据源时不抛异常
      datasource:
        master:
          url: jdbc:mysql://主库IP:3306/cosplay_schedule
          username: root
          password: 主库密码
        slave:
          url: jdbc:mysql://从库IP:3306/cosplay_schedule
          username: readonly  # 从库只配读权限账号
          password: 从库密码
```

```java
// 1. 读操作加 @DS("slave") 走从库，写操作默认走主库（无需注解）
@Service
public class MerchantService {

    // 写操作：不加注解，走默认主库
    @Transactional
    public void updateMerchant(Long id, UpdateMerchantDTO dto) {
        merchantMapper.updateById(convert(dto));
        merchantCacheService.evict(id);
    }

    // 读操作：加 @DS("slave") 走从库
    @DS("slave")
    public Page<MerchantVO> searchMerchants(SearchDTO dto) {
        // ...
    }
}

// 2. 也可以加在 Mapper 方法上（更细粒度）
@Mapper
public interface MerchantMapper extends BaseMapper<Merchant> {

    @DS("slave")
    @Select("SELECT * FROM merchant WHERE city = #{city}")
    List<Merchant> selectByCity(@Param("city") String city);
}

// 3. 注意：@Transactional 内部强制走主库（事务必须在同一连接）
// 下面这个例子，即使方法上有 @DS("slave")，因为有事务会自动切回主库
@Transactional
@DS("slave")  // 这里的 @DS 在 @Transactional 内会被忽略
public void doSomething() { ... }
```

**主从延迟问题**：MySQL 主从同步有毫秒级延迟，刚写完主库立刻查从库可能还没同步到。对于"下单成功后立刻查订单详情"这种场景，必须强制读主库。

```java
// 对实时性要求高的读操作，强制走主库
@DS("master")
public OrderVO getOrderDetail(Long orderId) { ... }

// 或者在 Service 层用编程方式切换（不推荐，但特殊场景有用）
DynamicDataSourceContextHolder.push("master");
try {
    return orderMapper.selectById(orderId);
} finally {
    DynamicDataSourceContextHolder.poll();
}
```

### 面试题

**Q：读写分离后如何处理主从延迟问题？**

A：三种方案：① 对实时性要求高的读操作（如查刚创建的订单），强制走主库；② 引入缓存，写完主库同时更新缓存，读操作查缓存而不是从库，绕过主从延迟；③ 在业务上做妥协，提示用户"数据更新需要几秒后刷新"。本项目订单详情走主库，商家列表走从库，是典型的折中方案。

**Q：`@Transactional` 和 `@DS` 同时存在时哪个优先？**

A：`@Transactional` 优先级更高。Spring 事务要求同一个事务里所有操作在同一个数据库连接上，事务开始时会绑定数据源（通常是主库），后续即使有 `@DS("slave")` 也无法切换。这是读写分离的一个重要限制：**事务内强制走主库**。

---

## 第二十三阶段：本地消息表——保证 MQ 消息必达（第26天）

### 要做什么

第八阶段 RabbitMQ 开了 Confirm 模式，但如果业务代码抛了异常，事务回滚了，但消息已经发出去了（或者反过来：消息没发成功，但数据库已经写入了），出现数据不一致。**本地消息表**是保证"数据库操作"和"发 MQ 消息"最终一致性的工业标准方案，比分布式事务（Seata）轻量得多。

### 要学什么

**核心思路**：把"消息发送记录"和"业务数据"存在同一个数据库事务里，之后由定时任务扫描未发送的消息重试，直到成功。

```sql
-- 本地消息表
CREATE TABLE outbox_message (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    msg_id VARCHAR(32) NOT NULL UNIQUE,       -- 消息唯一ID（用于幂等）
    exchange VARCHAR(100) NOT NULL,
    routing_key VARCHAR(100) NOT NULL,
    payload TEXT NOT NULL,                    -- 消息内容 JSON
    status TINYINT DEFAULT 0 COMMENT '0:待发送 1:已发送 2:发送失败',
    retry_count INT DEFAULT 0,
    create_time DATETIME,
    next_retry_time DATETIME,                 -- 下次重试时间（指数退避）
    KEY idx_status_retry (status, next_retry_time)
);
```

```java
// 1. 下单时：数据库操作 + 写消息表 在同一个事务里
@Transactional
public void createOrder(CreateOrderDTO dto) {
    // 业务操作：写订单表
    Order order = buildOrder(dto);
    orderMapper.insert(order);

    // 同事务：写本地消息表（消息还没发出去）
    OutboxMessage msg = new OutboxMessage();
    msg.setMsgId(IdUtil.fastSimpleUUID());
    msg.setExchange("schedule.exchange");
    msg.setRoutingKey("notify.rush");
    msg.setPayload(JSONUtil.toJsonStr(buildNotifyMsg(order)));
    outboxMessageMapper.insert(msg);

    // 不在事务里直接发 MQ！由异步任务来发
}

// 2. XXL-Job 定时任务：扫描待发送消息，发出并更新状态
@XxlJob("outboxMessageJob")
public void outboxMessageJob() {
    List<OutboxMessage> messages = outboxMessageMapper.selectPending(
        LocalDateTime.now(), 100  // next_retry_time <= now，取100条
    );

    for (OutboxMessage msg : messages) {
        try {
            // 消费者端已做幂等（Redis SET NX msgId），重复发送安全
            rabbitTemplate.convertAndSend(msg.getExchange(), msg.getRoutingKey(),
                JSONUtil.parseObj(msg.getPayload()));

            // 发送成功：更新状态
            outboxMessageMapper.markSent(msg.getId());

        } catch (Exception e) {
            // 发送失败：指数退避重试（1分钟、2分钟、4分钟……）
            int retryCount = msg.getRetryCount() + 1;
            LocalDateTime nextRetry = LocalDateTime.now().plusMinutes((long) Math.pow(2, retryCount));
            outboxMessageMapper.markFailed(msg.getId(), retryCount, nextRetry);
        }
    }
}
```

### 面试题

**Q：本地消息表和 RabbitMQ 事务消息的区别？**

A：RabbitMQ 事务消息（`channel.txSelect()`）是同步的，性能极差，几乎不在生产使用。本地消息表是异步的：先写库，异步发 MQ，性能损耗极小。缺点是有轻微延迟（从写库到消息发出可能有几秒），适合对最终一致性有要求、对实时性要求不高的场景（如通知类）。

**Q：如果本地消息表的消息一直失败，怎么处理？**

A：设置最大重试次数（如 5 次），超过后标记为 `status=2`（永久失败），并发告警。告警后人工介入或通过补偿脚本处理。同时记录详细的错误信息方便排查根因。这比让消息无限重试占用资源更合理。

---

## 第二十四阶段：Java 21 虚拟线程（Spring Boot 3.2 一行开启）

### 要做什么

Java 21 正式引入虚拟线程（Virtual Thread），Spring Boot 3.2 开箱支持。**一行配置**开启后，所有 HTTP 请求、`@Async` 任务、定时任务都跑在虚拟线程上，IO 等待时自动挂起释放平台线程，吞吐量可提升数倍。这是面试热点，公司已在生产落地。

### 要学什么

**开启方式：**

```yaml
# application.yml — 一行配置，Spring Boot 3.2 + Java 21 即可生效
spring:
  threads:
    virtual:
      enabled: true
```

```java
// 或者用 Bean 配置方式
@Bean
public TomcatProtocolHandlerCustomizer<?> virtualThreadCustomizer() {
    return handler -> handler.setExecutor(Executors.newVirtualThreadPerTaskExecutor());
}
```

**平台线程 vs 虚拟线程：**

```
平台线程（传统）：
  JVM线程 1:1 对应 OS线程
  线程阻塞时 OS线程也阻塞
  创建/切换代价大（MB级栈内存）
  Tomcat 默认200个线程 → 最多200并发

虚拟线程（Java 21）：
  M:N 映射，数百万虚拟线程 → 少量平台线程（Carrier Threads）
  IO阻塞时虚拟线程挂起，平台线程继续处理其他虚拟线程
  创建代价极小（KB级），可创建数百万个
  相同物理资源下，IO密集场景吞吐量提升 5-10 倍
```

```java
// 对比测试：用虚拟线程处理1000个并发数据库查询
try (var executor = Executors.newVirtualThreadPerTaskExecutor()) {
    List<Future<MerchantVO>> futures = IntStream.range(0, 1000)
        .mapToObj(i -> executor.submit(() -> merchantService.getMerchantVO((long) i)))
        .toList();
    // 1000个查询并行执行，虚拟线程等IO时自动让出平台线程
}

// Java 21 结构化并发（Structured Concurrency，更优雅的 CompletableFuture 替代）
try (var scope = new StructuredTaskScope.ShutdownOnFailure()) {
    var merchantTask = scope.fork(() -> merchantService.getMerchantVO(merchantId));
    var reviewTask   = scope.fork(() -> reviewService.getLatestReviews(merchantId, 5));
    var postTask     = scope.fork(() -> postService.getLatestPosts(merchantId, 6));

    scope.join();          // 等所有任务
    scope.throwIfFailed(); // 任一失败则抛出

    return new MerchantDetailVO(merchantTask.get(), reviewTask.get(), postTask.get());
}
```

**虚拟线程的限制（面试必知）：**
- 不适合 CPU 密集计算（计算密集靠平台线程，虚拟线程切换反而有开销）
- `synchronized` 会导致虚拟线程"固定（Pin）"在平台线程上，阻塞期间平台线程无法释放。改用 `ReentrantLock` 解决
- `ThreadLocal` 在虚拟线程数量巨大时内存占用更明显，考虑使用 `ScopedValue`（Java 21 孵化期）

```java
// ⚠️ 避免在虚拟线程中用 synchronized，改用 ReentrantLock
private final ReentrantLock lock = new ReentrantLock();

public void doSomething() {
    lock.lock();
    try {
        // IO 等待时，虚拟线程可以挂起，平台线程去处理其他请求
    } finally {
        lock.unlock();
    }
}
```

### 验收标准

- `application.yml` 加一行，重启项目
- 用 Jmeter 发 500 并发请求，对比开启前后的吞吐量（QPS）
- 在控制台打印线程名，能看到 `VirtualThread[#xxx]` 格式

### 面试题

**Q：虚拟线程和协程（Kotlin Coroutine）的区别？**

A：概念类似，都是为了解决 IO 密集的高并发问题。虚拟线程是 JVM 级别的，对代码透明，不需要改写为异步风格（`suspend`/`async`），现有阻塞式代码直接受益。Kotlin 协程需要代码配合（用 `suspend` 标记函数），侵入性更强，但可组合性更好（结构化并发更完善）。对于现有 Spring MVC 代码，虚拟线程是零侵入的迁移路径。

**Q：开启虚拟线程后，线程池还需要吗？**

A：IO 密集场景可以减小线程池大小甚至不用线程池（直接 `newVirtualThreadPerTaskExecutor`）。但 CPU 密集任务（图像处理、加密计算）仍然需要有界的平台线程池，不然创建太多虚拟线程反而因 CPU 争抢降低性能。本项目 IO 密集（数据库/Redis/MQ），开启虚拟线程后 Tomcat 线程池和自定义 IO 线程池都可以相应缩小。

---

## 第二十五阶段：分布式链路追踪——SkyWalking（第50天）

### 要做什么

一个请求经过 Nginx → Spring Boot → Redis → MySQL → RabbitMQ，出了问题很难定位在哪一环。SkyWalking 自动采集每个环节的耗时和调用链，在 Dashboard 一眼看出瓶颈在哪。这是互联网公司标配的可观测性基础设施。

### 要学什么

**部署方式（docker-compose 加一项）：**

```yaml
# docker-compose.yml 新增
elasticsearch:
  image: elasticsearch:8.11.0
  environment:
    - discovery.type=single-node
    - xpack.security.enabled=false
  ports:
    - "9200:9200"

skywalking-oap:
  image: apache/skywalking-oap-server:9.7.0
  environment:
    SW_STORAGE: elasticsearch
    SW_STORAGE_ES_CLUSTER_NODES: elasticsearch:9200
  ports:
    - "11800:11800"  # gRPC，Agent上报用
    - "12800:12800"  # HTTP，UI查询用
  depends_on:
    - elasticsearch

skywalking-ui:
  image: apache/skywalking-ui:9.7.0
  environment:
    SW_OAP_ADDRESS: http://skywalking-oap:12800
  ports:
    - "8888:8080"
```

**接入方式：Java Agent，代码零侵入**

```bash
# 下载 SkyWalking Agent
wget https://archive.apache.org/dist/skywalking/java-agent/9.1.0/apache-skywalking-java-agent-9.1.0.tgz

# 启动时加 -javaagent 参数（不改一行代码）
java -javaagent:/path/to/skywalking-agent/skywalking-agent.jar \
     -Dskywalking.agent.service_name=cosplay-backend \
     -Dskywalking.collector.backend_service=localhost:11800 \
     -jar app.jar
```

**IDEA 本地开发配置（VM options）：**

```
-javaagent:D:/skywalking-agent/skywalking-agent.jar
-Dskywalking.agent.service_name=cosplay-backend
-Dskywalking.collector.backend_service=localhost:11800
```

**自定义追踪（给业务方法打 Span）：**

```java
// 非必须，默认已追踪所有 Spring MVC / MyBatis / Redis / RabbitMQ 调用
// 如需追踪自定义业务逻辑：
import org.apache.skywalking.apm.toolkit.trace.Trace;

@Trace(operationName = "rushBooking")
public long rush(Long scheduleId, Long userId, int maxSize) {
    // 这个方法会出现在链路追踪的调用链中
}
```

**访问 `localhost:8888` 可以看到：**
- 每次请求的完整调用链（Nginx→Controller→Service→Redis→MySQL耗时）
- 慢查询告警
- 接口成功率和 P95/P99 延迟

### 面试题

**Q：SkyWalking 是如何做到零代码侵入的？**

A：利用 Java Agent 机制（`-javaagent` 参数）在 JVM 启动时修改字节码。SkyWalking Agent 通过字节码增强（Byte Buddy 框架）在 Spring MVC 的 Controller、MyBatis 的 Executor、Redis 的 Jedis/Lettuce 客户端等关键类的方法入口/出口插入追踪代码，应用代码完全感知不到。这和 Spring AOP 的代理方式不同，AOP 只能增强 Spring Bean，Agent 可以增强任何类。

**Q：分布式链路追踪的 TraceId 是如何跨服务传递的？**

A：生产者在发起 HTTP 请求时，SkyWalking Agent 自动往 Header 里注入 `sw8` 字段（包含 TraceId、SpanId 等），下游服务的 Agent 自动从 Header 提取并延续同一条链路。对于 MQ 消息，Agent 类似地将追踪信息注入消息的 Header 里，消费者提取后继续同一 Trace。整个过程对业务代码透明。

---

## 第二十六阶段：Prometheus + Grafana 监控（第50天，与SkyWalking并行）

### 要做什么

SkyWalking 看的是链路（一次请求的调用过程），Prometheus + Grafana 看的是指标（整体的 QPS、成功率、JVM 内存、线程数随时间的趋势）。两者互补，是生产环境标配监控体系。

### 要学什么

```xml
<!-- Spring Boot Actuator + Micrometer -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-actuator</artifactId>
</dependency>
<dependency>
    <groupId>io.micrometer</groupId>
    <artifactId>micrometer-registry-prometheus</artifactId>
</dependency>
```

```yaml
# application.yml
management:
  endpoints:
    web:
      exposure:
        include: prometheus,health,info,metrics   # 暴露 /actuator/prometheus 端点
  metrics:
    tags:
      application: cosplay-backend   # 所有指标加 application 标签
```

```yaml
# docker-compose.yml 新增 Prometheus 和 Grafana
prometheus:
  image: prom/prometheus:v2.48.0
  ports:
    - "9090:9090"
  volumes:
    - ./prometheus.yml:/etc/prometheus/prometheus.yml

grafana:
  image: grafana/grafana:10.2.0
  ports:
    - "3000:3000"
  environment:
    GF_SECURITY_ADMIN_PASSWORD: admin123
```

```yaml
# prometheus.yml — 告诉 Prometheus 去哪里拉数据
global:
  scrape_interval: 15s
scrape_configs:
  - job_name: 'cosplay-backend'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080']  # Windows/Mac Docker访问宿主机
```

**自定义业务指标（Micrometer）：**

```java
@Service
public class OrderService {

    private final Counter orderCreatedCounter;
    private final Counter orderCancelledCounter;
    private final Timer rushBookingTimer;

    public OrderService(MeterRegistry registry) {
        orderCreatedCounter = Counter.builder("order.created.total")
            .description("创建订单总数").register(registry);
        orderCancelledCounter = Counter.builder("order.cancelled.total")
            .description("取消订单总数").register(registry);
        rushBookingTimer = Timer.builder("rush.booking.duration")
            .description("抢档期接口耗时").register(registry);
    }

    public void createOrder(CreateOrderDTO dto) {
        // ...
        orderCreatedCounter.increment();  // 每次下单计数+1
    }

    public long rush(Long scheduleId, Long userId) {
        return rushBookingTimer.record(() -> doRush(scheduleId, userId));  // 记录耗时
    }
}
```

**Grafana 配置（访问 `localhost:3000`）：**
1. 添加 Prometheus 数据源（`http://prometheus:9090`）
2. 导入 JVM 仪表盘（ID: 4701）→ 一键获得 JVM 内存、GC、线程监控面板
3. 自定义面板：`rate(order_created_total[5m])` 展示每分钟下单量

### 面试题

**Q：Prometheus 的拉模式（Pull）和推模式（Push）有什么区别？**

A：Prometheus 默认是拉模式（Pull），定时去应用的 `/actuator/prometheus` 端点拉取指标。优点是 Prometheus 控制采集频率，应用挂了拉不到数据立刻感知，便于告警。缺点是短生命周期的任务（批处理作业、Lambda 函数）可能在被拉取前就结束了。对于这种场景用 Pushgateway：任务完成前主动 Push 指标到网关，Prometheus 再从网关 Pull。

**Q：项目中监控了哪些核心指标？**

A：四类：① JVM 指标（堆内存使用率、GC 次数和时间、线程数），由 Micrometer 自动采集；② 应用指标（下单量、取消量、抢档期接口 P95 延迟），自定义 Counter/Timer；③ 数据库指标（HikariCP 连接池活跃/等待连接数），Micrometer 自动采集；④ Redis 指标（命令执行次数、Lettuce 连接数），Micrometer 自动采集。出了问题先看 Grafana 确认影响范围，再用 SkyWalking 定位具体哪次请求出了问题，两者配合使用。
