# MHP 档期预约平台 — 完整功能测试清单

> 测试顺序：按章节顺序执行，前置章节是后续章节的依赖。
> 测试工具：**自动化脚本**（接口）+ 浏览器（前端）+ Redis CLI / MySQL CLI（验证中间层）。

---

## 自动化测试（一键运行）

脚本文件：`test-api.mjs`（项目根目录）

```bash
# 要求：Node 18+，后端 Gateway 已启动在 localhost:8080
node test-api.mjs
```

**覆盖范围（12 个模块，约 90 个用例）：**

| 模块 | 主要测试点 |
|------|-----------|
| 用户认证 | 重复注册、密码错误、无 token 返回 401 |
| 商家管理 | 更新资料、缓存命中、防穿透、搜索 |
| 档期管理 | 创建/全天/重复/批量（空 weekdays）/权限隔离 |
| 预约流程 | 完整状态机（发起→确认→完成）+ 越权操作 |
| 抢档期 | 加入/重复/满员/名次/商家更新状态 |
| 社区动态 | 发布/点赞幂等/删除权限 |
| 关注 | 关注/幂等/查询状态/取消 |
| 评价 | 非法评价/正常评价/重复/avgScore 更新/商家回复 |
| 投诉 | 提交/商家查收到列表 |
| 消息 | 发送/会话列表/游标分页历史 |
| 权限隔离 | 无 token/白名单/跨商家操作/路径参数校验 |
| 问卷 | 创建模板/查询/携带答案预约/删除 |
| 聊天入口 | BookingVO.merchantUserId 字段/首次发消息/空历史/双向对话/鉴权 |

**特点：**
- 自动注册 4 个测试账号（幂等，可重复运行）
- 多账号场景（客人 + 商家）全部脚本内切换，无需手动操作
- 动态生成未来日期，不与生产数据冲突
- 彩色 ✓/✗ 输出，最终打印失败用例列表
- 退出码：全部通过为 0，有失败为 1（可接入 CI）

**不覆盖（需手动）：**
- WebSocket 实时推送（需浏览器）
- Canal CDC 延迟同步（需等待）
- XXL-Job 定时任务（需控制台触发）
- 前端 UI 交互（需浏览器）

---

---

## 准备工作

### 环境启动检查

| 检查项 | 命令 | 预期结果 |
|--------|------|---------|
| 所有 Docker 容器运行中 | `docker compose ps` | 全部 `running`（Canal 可能 `restarting`，等 MySQL healthy 后自稳） |
| MySQL 可连 | `docker exec mhp-mysql mysql -uroot -p222333dyh -e "show databases;"` | 包含 `mhp` 和 `xxl_job` |
| Redis 可连 | `docker exec mhp-redis redis-cli ping` | `PONG` |
| Nacos 可访问 | 浏览器 `http://localhost:8848/nacos` | 登录页 |
| RabbitMQ 可访问 | 浏览器 `http://localhost:15672`（admin/123456）| 管理台 |
| ES 可访问 | `curl http://localhost:9200/_cluster/health` | `status: green` 或 `yellow` |
| 三个微服务注册到 Nacos | Nacos 控制台 → 服务列表 | `mhp-account`、`mhp-booking`、`mhp-social` 均在列 |
| Gateway 路由正常 | `curl http://localhost:8080/api/user/health`（或任意白名单接口）| 非 502 |

### 测试账号规划

测试前手动注册以下账号（用于不同角色的测试）：

| 角色 | 用户名 | 密码 | 用途 |
|------|--------|------|------|
| 普通用户 A（客人）| `user_a` | `Test1234` | 发起预约、评价 |
| 普通用户 B（客人）| `user_b` | `Test1234` | 测试权限隔离 |
| 商家用户 C | `merchant_c` | `Test1234` | 管理档期、接收预约 |
| 商家用户 D | `merchant_d` | `Test1234` | 测试跨商家权限隔离 |

---

## 一、用户模块

### 1.1 注册

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 1 | 正常注册 | `POST /api/user/register`，填写合法用户名/密码 | 注册成功，返回 200 |
| 2 | 用户名重复 | 相同用户名再次注册 | 返回错误码，提示用户名已存在 |
| 3 | 密码为空 | password 字段不传 | Bean Validation 校验失败，返回 400 |
| 4 | 未登录访问受保护接口 | 不带 token 调用 `GET /api/booking` | 返回 401 |

### 1.2 登录 / 登出

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 5 | 正常登录 | `POST /api/user/login`，正确账密 | 返回 token |
| 6 | 密码错误 | 错误密码登录 | 返回错误，不返回 token |
| 7 | 用户不存在 | 未注册用户名登录 | 返回错误 |
| 8 | 登出后 token 失效 | 登出后用旧 token 请求 | 返回 401 |

### 1.3 用户信息

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 9 | 获取当前用户信息 | `GET /api/user/me`（带 token）| 返回当前登录用户信息 |
| 10 | 修改昵称/头像 | `PUT /api/user/profile` | 修改成功；若 Canal 已启动，ES 中该商家 doc 的 nickname 同步更新 |

---

## 二、商家模块

### 2.1 商家身份

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 11 | 开通商家身份 | 用 merchant_c 账号调用开通商家接口，填写昵称/城市/服务类型/简介 | 成功，merchant 表新增记录 |
| 12 | 重复开通 | 已开通的账号再次开通 | 返回错误 |

### 2.2 商家信息管理

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 13 | 获取自己的商家信息 | `GET /api/merchant/my` | 返回当前商家信息 |
| 14 | 修改商家信息 | `PUT /api/merchant` 修改简介/城市 | 修改成功；Redis 中该商家缓存被删除 |
| 15 | 缓存命中验证 | 先调 `GET /api/merchant/{id}`（冷启动），再调一次 | 第一次查 MySQL，第二次 Redis 命中（可通过 Redis CLI `keys merchant:*` 验证） |
| 16 | 缓存空值防穿透 | 访问不存在的商家 ID | 返回空或错误；Redis 中写入空值哨兵，TTL 约 2 分钟 |

---

## 三、商家搜索（Elasticsearch）

> 前置：`POST http://localhost:8081/internal/merchant/es/init` 完成全量初始化。

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 17 | 关键词搜索 | `GET /api/merchant/search?keyword=妆娘` | 返回昵称或简介包含"妆娘"的商家列表，按相关度排序 |
| 18 | 城市 filter | `GET /api/merchant/search?city=北京` | 仅返回城市=北京的商家 |
| 19 | 服务类型 filter | `GET /api/merchant/search?serviceType=1` | 仅返回包含该服务类型的商家 |
| 20 | 组合条件 | `keyword=妆娘&city=北京&serviceType=1` | 三个条件同时生效 |
| 21 | 无 keyword 排序 | 不传 keyword，只传 city | 按 avgScore 降序排列 |
| 22 | Canal CDC 同步 | 修改商家昵称后等待 2~5 秒，再次搜索 | ES 中 doc 的 nickname 已更新 |

---

## 四、档期管理

### 4.1 创建档期

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 23 | 创建直接预约档期 | `POST /api/schedule`，bookType=0，指定日期和时间段 | 创建成功，月历中显示绿色 |
| 24 | 创建抢档期 | bookType=1，填写 rushOpenTime 和 maxQueueSize | 创建成功，月历中显示蓝色"抢"角标 |
| 25 | 日期缺失 | date 字段不传 | 校验失败，返回 400 |

### 4.2 批量创建

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 26 | 批量创建（全部日期）| `POST /api/schedule/batch`，指定日期范围，weekdays 不传 | 范围内每天均创建档期 |
| 27 | 批量创建（限定星期）| weekdays=[1,3,5]（周一三五）| 仅周一、周三、周五创建 |
| 28 | 部分日期已存在 | 范围内有的日期已有档期 | 已有日期跳过，其余正常创建（或返回冲突提示） |

### 4.3 查看与删除

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 29 | 按月查询 | `GET /api/schedule?merchantId=x&month=2026-09` | 返回该月所有档期，前端月历正确渲染 |
| 30 | 删除空闲档期 | 删除 status=0 的档期 | 删除成功 |
| 31 | 删除已预约档期 | 尝试删除 status=1 的档期 | 返回错误，不允许删除 |
| 32 | 删除他人档期 | 用 merchant_d 删除 merchant_c 的档期 | 返回 403 |

---

## 五、预约流程

### 5.1 发起预约

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 33 | 正常预约 | user_a 对 merchant_c 的空闲档期发起预约 | 创建成功，status=0（待确认），档期 status 变为 1（已预约）|
| 34 | 重复预约 | user_a 再次预约同一档期 | 返回错误 `BOOKING_DUPLICATE` |
| 35 | 预约他人已预约的档期 | user_b 预约同一档期 | 返回错误（档期不可用）|
| 36 | 预约不存在的档期 | scheduleId 传不存在的值 | 返回错误 |
| 37 | 分布式锁验证 | 两个请求并发预约同一档期 | 只有一个成功，另一个失败（验证 Redisson 锁生效）|

### 5.2 状态流转

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 38 | 商家确认预约 | merchant_c 调用 `PUT /api/booking/{id}/confirm` | status 变 2（已定档），RabbitMQ 发出通知消息 |
| 39 | 非商家确认 | user_a 调用 confirm | 返回 403 |
| 40 | 完成预约 | 已定档状态下调用 `PUT /api/booking/{id}/complete` | status 变 3（已完成），MQ 发通知 |
| 41 | 客人取消（待确认）| user_a 取消 status=0 的预约 | status 变 4（已取消），MQ 发通知 |
| 42 | 商家取消（已定档）| merchant_c 取消 status=2 的预约 | status 变 4 |
| 43 | 取消已完成预约 | 对 status=3 调用 cancel | 返回错误 `BOOKING_STATUS_ERROR` |
| 44 | 操作他人预约 | user_b 取消 user_a 的预约 | 返回 403 |

### 5.3 预约列表

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 45 | 客人查自己的预约 | `GET /api/booking/my`（user_a）| 仅返回 user_a 的预约，游标分页正确 |
| 46 | 商家查收到的预约 | `GET /api/booking/received`（merchant_c）| 仅返回发给 merchant_c 的预约 |
| 47 | 游标翻页 | 第一页末尾的 nextCursor 作为下页 lastId | 第二页数据不重复，hasMore 正确 |

---

## 六、抢档期

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 48 | 加入队列 | user_a 对抢档期调用 rush 接口 | 返回排队名次（正整数）|
| 49 | 重复加入 | user_a 再次加入同一档期队列 | 返回 -1（已在队列）|
| 50 | user_b 加入 | user_b 加入同一档期 | 返回排名 2 |
| 51 | 队列满员 | maxQueueSize=2，第三个用户加入 | 返回 0（队满）|
| 52 | 并发抢档期 | 多个用户并发 rush | Lua 脚本原子性保证不超员，名次不重复 |
| 53 | 商家查看排队名单 | `GET /api/schedule/{id}/queue` | 返回排队用户列表，含昵称和排名 |
| 54 | 商家更新排队状态 | 将某用户状态改为"中签" | 更新成功 |

---

## 七、问卷

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 55 | 商家创建问卷模板 | `POST /api/questionnaire`，填写题目列表 | 创建成功 |
| 56 | 获取问卷模板 | `GET /api/questionnaire/{merchantId}` | 返回该商家的问卷题目 |
| 57 | 预约时提交答案 | 发起预约时携带问卷答案 | 预约成功，答案保存 |
| 58 | 商家查看答案 | 查看预约详情 | 问卷答案正确显示 |

---

## 八、定时任务（XXL-Job）

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 59 | 超时取消 | 在 XXL-Job 控制台手动触发 `cancelTimeoutBookingJob` | 超过超时时限且仍为待确认的预约自动变为已取消 |
| 60 | 每日提醒 | 手动触发 `reminderJob` | 次日有预约的用户收到 MQ 消息（WebSocket 推送或日志中可见）|

> 手动触发路径：XXL-Job Admin（`http://localhost:8088/xxl-job-admin`）→ 任务管理 → 操作 → 执行一次

---

## 九、社区动态

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 61 | 发布动态 | `POST /api/post`，填写内容（可含图片 URL）| 发布成功 |
| 62 | 查看动态列表 | `GET /api/post/list`，游标分页 | 返回动态列表，hasMore 正确 |
| 63 | 点赞 | `POST /api/post/{id}/like` | 点赞成功，likeCount +1 |
| 64 | 重复点赞 | 同一用户对同一动态再次点赞 | 幂等，likeCount 不变（Redis Set 去重）|
| 65 | 取消点赞 | `DELETE /api/post/{id}/like` | 取消成功，likeCount -1 |
| 66 | 删除动态 | 作者删除自己的动态 | 删除成功 |
| 67 | 删除他人动态 | 非作者删除 | 返回 403 |

---

## 十、关注

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 68 | 关注商家 | user_a 关注 merchant_c | 成功，follow 表新增记录 |
| 69 | 重复关注 | 再次关注同一商家 | 幂等，不报错，不重复写入 |
| 70 | 取消关注 | user_a 取消关注 merchant_c | 成功，follow 记录删除 |
| 71 | 查看我的关注列表 | `GET /api/follow/following` | 返回 user_a 关注的商家列表 |
| 72 | 查看粉丝列表 | `GET /api/follow/followers` | 返回关注 merchant_c 的用户列表 |

---

## 十一、评价

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 73 | 正常评价 | user_a 对已完成（status=3）的预约发起评价 | 评价成功；merchant_c 的 avgScore 更新（跨服务调用 AccountFeign）|
| 74 | 对未完成预约评价 | 对 status=2 的预约发起评价 | BookingFeign 跨服务校验失败，返回错误 |
| 75 | 重复评价 | 同一预约再次评价 | 返回错误 |
| 76 | 查看商家评价列表 | `GET /api/review/merchant/{id}` | 返回该商家所有评价，含评分和内容 |

---

## 十二、投诉

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 77 | 发起投诉 | 对已完成预约发起投诉，填写原因 | 投诉成功，complaint 表新增记录 |
| 78 | 对未完成预约投诉 | 对 status=0 的预约投诉 | 跨服务校验失败，返回错误 |

---

## 十三、消息

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 79 | 发送消息 | user_a 向 merchant_c 发送文字消息 | 消息成功保存 |
| 80 | 查看会话列表 | `GET /api/message/sessions` | 返回 user_a 的所有会话，含最新一条消息预览 |
| 81 | 查看消息记录 | `GET /api/message/{targetId}`，游标分页 | 返回与 merchant_c 的消息历史，翻页正确 |
| 82 | WebSocket 实时收消息 | 两个浏览器分别登录 user_a 和 merchant_c，互发消息 | 对方实时收到，无需刷新 |
| 83 | 未读数更新 | merchant_c 收到新消息时 | AppHeader 的消息气泡数字 +1 |

---

## 十四、WebSocket 通知（MQ → WS）

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 84 | 预约确认通知 | merchant_c confirm 预约 | user_a 的浏览器收到系统通知"预约已确认" |
| 85 | 预约取消通知 | 任一方 cancel 预约 | 对方收到通知 |
| 86 | 预约完成通知 | complete 预约 | user_a 收到通知 |
| 87 | MQ 幂等去重 | 模拟重复投递同一 msgId（Redis SET NX 去重）| 消息只处理一次，WebSocket 只推一条 |

---

## 十五、鉴权与权限隔离

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 88 | 白名单接口无需 token | 不带 token 访问 `/api/user/login` | 正常响应 |
| 89 | 受保护接口无 token | 不带 token 访问 `/api/booking` | 401 |
| 90 | token 过期 | 等待 token 过期后请求 | 401，前端跳转登录页 |
| 91 | 商家操作自己的档期 | merchant_c 管理自己的档期 | 成功 |
| 92 | 商家操作他人档期 | merchant_d 删除 merchant_c 的档期 | 403 |
| 93 | 客人调用商家接口 | user_a 调用 confirm | 403 |

---

## 十六、前端完整链路

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 94 | 注册登录 | 浏览器走完注册→登录流程 | 登录后跳转首页，AppHeader 显示昵称 |
| 95 | 搜索商家 | 首页输入关键词搜索 | 商家卡片列表正确渲染 |
| 96 | 商家主页月历 | 点进商家主页 | 月历正确显示本月档期颜色和图例 |
| 97 | 发起预约弹窗 | 点击空闲日期 | 弹出预约表单，填写后提交 |
| 98 | 我的预约页 | 切换到"我的预约" | 预约列表正确显示，状态 tag 颜色正确 |
| 99 | 商家档期管理页 | merchant_c 登录后进入档期管理 | 月历正确，创建/删除功能可用 |
| 100 | 消息页 | 进入消息页 | 会话列表加载，点入某会话显示历史消息 |
| 101 | 路由守卫 | 未登录直接访问 `/bookings` | 跳转到登录页 |
| 102 | 401 自动跳转 | token 失效后触发任意接口 | Axios 拦截器捕获 401，自动跳登录页 |
| 103 | 游标分页翻页 | 在消息列表或预约列表向上滚动加载 | 触底加载更多，无重复数据 |

---

## 十七、异常与边界

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 104 | Gateway 未注册服务 | 停掉 mhp-booking，访问 `/api/booking` | Gateway 返回 503 或 Sentinel 熔断响应，不返回 500 堆栈 |
| 105 | 传入非法参数类型 | 路径参数 `{id}` 传字母 | 返回 400，不报 500 |
| 106 | 超大请求体 | POST body 超出限制 | 返回 413 或自定义错误，不崩溃 |
| 107 | 并发创建档期 | 同一商家并发提交同一日期的创建请求 | 只有一个成功（数据库唯一索引兜底）|

---

---

## 十八、新增功能（本期迭代）

### 18.1 固定间隔批量创建档期

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 108 | 开启固定间隔模式 | 批量创建对话框 → 选"固定间隔自动生成" | 表单切换到开始/结束/间隔输入 |
| 109 | 实时预览时间段 | 填写开始 08:00、结束 20:00、间隔 2h | 立即显示 6 个标签：08:00-10:00、10:00-12:00 … 18:00-20:00 |
| 110 | 提交固定间隔创建 | 选日期范围 10.1-10.3，间隔设置如上，点批量创建 | 每天创建 6 个档期，共 18 个档期，月历中 10.1-10.3 均显示"6 个档期" |
| 111 | 间隔设置无法生成 | 开始时间 = 结束时间或 > 结束时间 | 预览区显示空，提交时弹出警告 |
| 112 | 0.5h 最小间隔 | 间隔设为 0.5，8:00-12:00 | 预览显示 8 个 30 分钟档期 |
| 113 | 全天档期（null timeSlot） | 单独创建勾选"全天" | 创建成功，不再报"时间段格式不正确" |
| 114 | weekdays 为空批量创建 | 批量创建不选限定星期 | 范围内每天均创建档期（不报 @Size 错误） |

### 18.2 已预约档期仍可点击查看

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 115 | 点击已预约（橙色）日期 | 商家主页月历 → 点击 status=1 的档期 | 弹出"档期信息"对话框，显示时间段和状态，无"立即预约"按钮 |
| 116 | 同天多个档期 | 一天有 2+ 个档期 | 日期格显示数字气泡（如 "3"），点击后弹出列表选择 |

### 18.3 商家档期可见度切换

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 117 | 商家设置"仅忙闲" | 档期管理页头部下拉框选"仅忙闲" | 调用成功；客人在商家主页只能看到哪天有档期，看不到时间段 |
| 118 | 商家设置"完全私密" | 选"完全私密" | 客人在商家主页看不到任何档期颜色 |
| 119 | 恢复"全公开" | 选"全公开" | 客人恢复正常查看完整档期 |

### 18.4 投诉可见度

| # | 场景 | 操作 | 预期结果 |
|---|------|------|---------|
| 120 | 客人提交投诉 | "我卖出的"→ 找一笔已完成预约 → 点"投诉"→ 填写原因 | 投诉成功；商家侧 WebSocket 收到"COMPLAINT_RECEIVED"通知 |
| 121 | 商家查看收到的投诉 | 切换到"我卖出的" → Tab 点击"收到的投诉" | 展示投诉列表：投诉方昵称、原因、状态标签、时间 |
| 122 | 投诉 Tab 时服务类型筛选隐藏 | 在"收到的投诉"Tab | 顶部服务类型芯片不显示（仅预约列表显示）|

---

## 测试完成标准

- [ ] `node test-api.mjs` 全部用例通过（退出码 0）
- [ ] 上述手动用例均通过（或已知 limitation 已记录）
- [ ] Nacos 服务列表始终显示三个微服务
- [ ] ES 商家数据与 MySQL 保持一致（Canal 同步正常）
- [ ] RabbitMQ 管理台 queues 无积压（消息正常消费）
- [ ] 无 Java OOM 或 Fatal 日志
