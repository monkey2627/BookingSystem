# 开发/部署问题记录

> 记录实际遇到的坑，含完整的现象→排查→根因→修复链路，适合面试"遇到过什么困难"类问题。

---

## 问题一：所有接口第一次请求必定 404，第二次才成功

### 现象

部署上线后，**任何接口的第一次请求都返回 404**，页面提示"网络异常请重试"，点第二次立即成功。切换不同参数重试，依然是"第一次失败、第二次成功"的固定模式，非常规律。

```
用户点击"注册" → 404（网络异常）
再次点击"注册" → 200（成功）

换手机号重新注册 → 404（网络异常）
再次点击"注册" → 200（成功）
```

### 排查过程

**第一反应：LoadBalancer 懒加载导致第一次请求实例列表为空。**

加了 `spring.cloud.loadbalancer.eager-load` 配置让 Gateway 启动时就拉取实例列表，但问题依旧。

**关键转折：** 意识到如果是懒加载问题，第一次成功后缓存已热，换参数的第一次应该也能成功——但实验证明不是这样，每次第一次都必然失败，说明不是随机的缓存问题，而是**确定性的轮询问题**。

**看 Gateway 日志：**

```bash
tail -f /home/ubuntu/BookingSystem/logs/gateway.log
```

发现关键信息：

```
init new ips(2) service: DEFAULT_GROUP@@mhp-account -> [
  {"ip":"10.2.0.6",  "port":8081, "metadata":{"preserved.register.source":"SPRING_CLOUD"}},
  {"ip":"172.18.0.1","port":50051,"metadata":{"dubbo.endpoints":"[{\"port\":50051}]",...}}
]
```

**mhp-account 在 Nacos 注册了两个实例**，一个是 Spring Boot HTTP（8081），另一个是 Dubbo Triple（50051）。

### 根本原因

Dubbo 和 Spring Cloud 都使用了 Nacos 作为注册中心，且**都注册到了默认的 `DEFAULT_GROUP`**：

- Spring Cloud 的 Nacos Discovery 将 mhp-account 的 HTTP 实例（8081）注册到 `DEFAULT_GROUP`
- Dubbo 的 registry 配置 `address: nacos://127.0.0.1:8848` 没有指定 group，也默认注册到 `DEFAULT_GROUP`，将 Triple 实例（50051）注册进来

Gateway 的 Spring Cloud LoadBalancer 在 `DEFAULT_GROUP` 里看到了两个实例，按照默认的**轮询（RoundRobin）**策略交替发请求：

```
请求1 → 50051（Dubbo Triple 端口，不接受 HTTP）→ 404
请求2 → 8081（Spring Boot HTTP）→ 成功
请求3 → 50051 → 404
请求4 → 8081 → 成功
...
```

这完美解释了"永远第一次失败、第二次成功"的规律——因为 Dubbo 实例恰好排在轮询序列的奇数位。

### 解决方案

给所有微服务的 Dubbo registry 指定独立的 `group`，让 Dubbo 实例和 Spring Cloud 实例在 Nacos 中隔离，互不干扰：

```yaml
# mhp-account / mhp-booking / mhp-social 的 application.yaml
dubbo:
  registry:
    address: nacos://127.0.0.1:8848
    group: DUBBO    # 与 Spring Cloud 的 DEFAULT_GROUP 隔离
```

**三个服务都要改**（提供者和消费者必须在同一个 group，否则消费者找不到提供者）。

改完后重新部署，Nacos 服务列表中 mhp-account 只剩一个 HTTP 实例，Gateway 不再轮询 Dubbo 端口，问题消失。

### 经验总结

| | 说明 |
|---|---|
| **根因** | Dubbo 和 Spring Cloud 共用同一个 Nacos group，导致 Gateway LoadBalancer 把 Dubbo 端口当 HTTP 实例轮询 |
| **误导方向** | 最初怀疑 LoadBalancer 懒加载，但懒加载修复后问题依旧，说明是轮询问题而非冷启动问题 |
| **关键诊断** | 查看 Gateway 日志中的 `init new ips` 行，发现同一服务有两个不同端口的实例 |
| **修复代价** | 低——只需在 YAML 加一行 `group: DUBBO`，无需改代码 |
| **举一反三** | 只要项目同时使用 Dubbo + Spring Cloud Nacos，且 Dubbo 不指定 group，都会有此风险 |

### 面试表述参考

> "我们项目同时用了 Dubbo 和 Spring Cloud，两者都接入 Nacos 做注册中心。上线后发现所有接口第一次必然 404，第二次成功，而且极其规律。最初以为是 LoadBalancer 懒加载，加了 eager-load 配置但没有解决。后来看 Gateway 日志发现 Nacos 里同一个服务名下有两个实例——一个是 Spring Boot 的 HTTP 端口，另一个是 Dubbo 的 Triple 端口。原来 Dubbo 和 Spring Cloud 都注册到了 Nacos 的默认 DEFAULT_GROUP，Gateway 的 RoundRobin 把两个端口轮流打，Dubbo 端口不接受 HTTP 就返回 404。修复方法很简单，给 Dubbo registry 指定一个独立的 group，让两套注册数据互不干扰。这个问题给我的教训是：多中间件共用同一注册中心时，必须注意命名空间/分组的隔离，否则会互相污染服务发现数据。"
